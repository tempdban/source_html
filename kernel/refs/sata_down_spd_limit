<dec f='linux-4.18.y/drivers/ata/libata.h' l='81' type='int sata_down_spd_limit(struct ata_link * link, u32 spd_limit)'/>
<use f='linux-4.18.y/drivers/ata/libata-core.c' l='2979' u='c' c='ata_bus_probe'/>
<def f='linux-4.18.y/drivers/ata/libata-core.c' l='3054' ll='3112' type='int sata_down_spd_limit(struct ata_link * link, u32 spd_limit)'/>
<doc f='linux-4.18.y/drivers/ata/libata-core.c' l='3034'>/**
 *	sata_down_spd_limit - adjust SATA spd limit downward
 *	@link: Link to adjust SATA spd limit for
 *	@spd_limit: Additional limit
 *
 *	Adjust SATA spd limit of @link downward.  Note that this
 *	function only adjusts the limit.  The change must be applied
 *	using sata_set_spd().
 *
 *	If @spd_limit is non-zero, the speed is limited to equal to or
 *	lower than @spd_limit if such speed is supported.  If
 *	@spd_limit is slower than any supported speed, only the lowest
 *	supported speed is allowed.
 *
 *	LOCKING:
 *	Inherited from caller.
 *
 *	RETURNS:
 *	0 on success, negative errno on failure
 */</doc>
<use f='linux-4.18.y/drivers/ata/libata-eh.c' l='2036' u='c' c='ata_eh_speed_down'/>
<use f='linux-4.18.y/drivers/ata/libata-eh.c' l='3038' u='c' c='ata_eh_reset'/>
<use f='linux-4.18.y/drivers/ata/libata-eh.c' l='3040' u='c' c='ata_eh_reset'/>
<use f='linux-4.18.y/drivers/ata/libata-eh.c' l='3042' u='c' c='ata_eh_reset'/>
<use f='linux-4.18.y/drivers/ata/libata-eh.c' l='3673' u='c' c='ata_eh_schedule_probe'/>
<use f='linux-4.18.y/drivers/ata/libata-eh.c' l='3702' u='c' c='ata_eh_handle_dev_fail'/>
<use f='linux-4.18.y/drivers/ata/libata-pmp.c' l='814' u='c' c='sata_pmp_eh_recover_pmp'/>
