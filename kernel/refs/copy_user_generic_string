<dec f='linux-4.18.y/arch/x86/include/asm/uaccess_64.h' l='23' type='unsigned long copy_user_generic_string(void * to, const void * from, unsigned int len)'/>
<use f='linux-4.18.y/arch/x86/include/asm/uaccess_64.h' l='38' c='copy_user_generic'/>
<def f='linux-4.18.y/arch/x86/lib/copy_user_64.S' l='136' macro='1' type='int'/>
<doc f='linux-4.18.y/arch/x86/lib/copy_user_64.S' l='118'>/* Some CPUs run faster using the string copy instructions.
 * This is also a lot simpler. Use them when possible.
 *
 * Only 4GB of copy is supported. This shouldn&apos;t be a problem
 * because the kernel normally only writes from/to page sized chunks
 * even if user space passed a longer buffer.
 * And more would be dangerous because both Intel and AMD have
 * errata with rep movsq &gt; 4GB. If someone feels the need to fix
 * this please consider this.
 *
 * Input:
 * rdi destination
 * rsi source
 * rdx count
 *
 * Output:
 * eax uncopied bytes or 0 if successful.
 */</doc>
