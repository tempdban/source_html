<def f='linux-4.18.y/include/linux/rhashtable.h' l='685' ll='696' type='void * rhashtable_lookup_fast(struct rhashtable * ht, const void * key, const struct rhashtable_params params)'/>
<doc f='linux-4.18.y/include/linux/rhashtable.h' l='671'>/**
 * rhashtable_lookup_fast - search hash table, without RCU read lock
 * @ht:		hash table
 * @key:	the pointer to the key
 * @params:	hash table parameters
 *
 * Computes the hash value for the key and traverses the bucket chain looking
 * for a entry with an identical key. The first matching entry is returned.
 *
 * Only use this function when you have other mechanisms guaranteeing
 * that the object won&apos;t go away after the RCU read lock is released.
 *
 * Returns the first entry on which the compare function returned true.
 */</doc>
<use f='linux-4.18.y/ipc/util.c' l='184' u='c' c='ipc_findkey'/>
<use f='linux-4.18.y/net/core/xdp.c' l='104' u='c' c='__xdp_rxq_info_unreg_mem_model'/>
<use f='linux-4.18.y/net/netlink/af_netlink.c' l='512' u='c' c='__netlink_lookup'/>
<use f='linux-4.18.y/security/apparmor/apparmorfs.c' l='702' u='c' c='query_data'/>
