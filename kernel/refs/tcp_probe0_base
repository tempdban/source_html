<def f='linux-4.14.y/include/net/tcp.h' l='1234' ll='1237' type='unsigned long tcp_probe0_base(const struct sock * sk)'/>
<use f='linux-4.14.y/include/net/tcp.h' l='1243' u='c' c='tcp_probe0_when'/>
<use f='linux-4.14.y/include/net/tcp.h' l='1252' u='c' c='tcp_check_probe_timer'/>
<doc f='linux-4.14.y/include/net/tcp.h' l='1228'>/* Something is really bad, we could not queue an additional packet,
 * because qdisc is full or receiver sent a 0 window.
 * We do not want to add fuel to the fire, or abort too early,
 * so make sure the timer we arm now is at least 200ms in the future,
 * regardless of current icsk_rto value (as it could be ~2ms)
 */</doc>
<def f='linux-4.14.y/include/net/tcp.h' l='1234' ll='1237' type='unsigned long tcp_probe0_base(const struct sock * sk)'/>
<use f='linux-4.14.y/include/net/tcp.h' l='1243' u='c' c='tcp_probe0_when'/>
<use f='linux-4.14.y/include/net/tcp.h' l='1252' u='c' c='tcp_check_probe_timer'/>
<doc f='linux-4.14.y/include/net/tcp.h' l='1228'>/* Something is really bad, we could not queue an additional packet,
 * because qdisc is full or receiver sent a 0 window.
 * We do not want to add fuel to the fire, or abort too early,
 * so make sure the timer we arm now is at least 200ms in the future,
 * regardless of current icsk_rto value (as it could be ~2ms)
 */</doc>
