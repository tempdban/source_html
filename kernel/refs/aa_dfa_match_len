<dec f='linux-4.14.y/security/apparmor/include/match.h' l='126' type='unsigned int aa_dfa_match_len(struct aa_dfa * dfa, unsigned int start, const char * str, int len)'/>
<use f='linux-4.14.y/security/apparmor/include/policy.h' l='218' u='c' c='PROFILE_MEDIATES_SAFE'/>
<use f='linux-4.14.y/security/apparmor/apparmorfs.c' l='616' u='c' c='profile_query_cb'/>
<use f='linux-4.14.y/security/apparmor/apparmorfs.c' l='628' u='c' c='profile_query_cb'/>
<use f='linux-4.14.y/security/apparmor/domain.c' l='113' u='c' c='match_component'/>
<use f='linux-4.14.y/security/apparmor/domain.c' l='115' u='c' c='match_component'/>
<use f='linux-4.14.y/security/apparmor/label.c' l='1257' u='c' c='match_component'/>
<use f='linux-4.14.y/security/apparmor/label.c' l='1259' u='c' c='match_component'/>
<def f='linux-4.14.y/security/apparmor/match.c' l='331' ll='367' type='unsigned int aa_dfa_match_len(struct aa_dfa * dfa, unsigned int start, const char * str, int len)'/>
<doc f='linux-4.14.y/security/apparmor/match.c' l='315'>/**
 * aa_dfa_match_len - traverse @dfa to find state @str stops at
 * @dfa: the dfa to match @str against  (NOT NULL)
 * @start: the state of the dfa to start matching in
 * @str: the string of bytes to match against the dfa  (NOT NULL)
 * @len: length of the string of bytes to match
 *
 * aa_dfa_match_len will match @str against the dfa and return the state it
 * finished matching in. The final state can be used to look up the accepting
 * label, or as the start state of a continuing match.
 *
 * This function will happily match again the 0 byte and only finishes
 * when @len input is consumed.
 *
 * Returns: final state reached after input is consumed
 */</doc>
<dec f='linux-4.14.y/security/apparmor/include/match.h' l='126' type='unsigned int aa_dfa_match_len(struct aa_dfa * dfa, unsigned int start, const char * str, int len)'/>
<use f='linux-4.14.y/security/apparmor/include/policy.h' l='218' u='c' c='PROFILE_MEDIATES_SAFE'/>
<use f='linux-4.14.y/security/apparmor/apparmorfs.c' l='616' u='c' c='profile_query_cb'/>
<use f='linux-4.14.y/security/apparmor/apparmorfs.c' l='628' u='c' c='profile_query_cb'/>
<use f='linux-4.14.y/security/apparmor/domain.c' l='113' u='c' c='match_component'/>
<use f='linux-4.14.y/security/apparmor/domain.c' l='115' u='c' c='match_component'/>
<use f='linux-4.14.y/security/apparmor/label.c' l='1257' u='c' c='match_component'/>
<use f='linux-4.14.y/security/apparmor/label.c' l='1259' u='c' c='match_component'/>
<def f='linux-4.14.y/security/apparmor/match.c' l='331' ll='367' type='unsigned int aa_dfa_match_len(struct aa_dfa * dfa, unsigned int start, const char * str, int len)'/>
<doc f='linux-4.14.y/security/apparmor/match.c' l='315'>/**
 * aa_dfa_match_len - traverse @dfa to find state @str stops at
 * @dfa: the dfa to match @str against  (NOT NULL)
 * @start: the state of the dfa to start matching in
 * @str: the string of bytes to match against the dfa  (NOT NULL)
 * @len: length of the string of bytes to match
 *
 * aa_dfa_match_len will match @str against the dfa and return the state it
 * finished matching in. The final state can be used to look up the accepting
 * label, or as the start state of a continuing match.
 *
 * This function will happily match again the 0 byte and only finishes
 * when @len input is consumed.
 *
 * Returns: final state reached after input is consumed
 */</doc>
