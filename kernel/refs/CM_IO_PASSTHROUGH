<dec f='linux-4.18.y/drivers/md/dm-cache-target.c' l='349' type='2'/>
<use f='linux-4.18.y/drivers/md/dm-cache-target.c' l='522' u='r' c='passthrough_mode'/>
<use f='linux-4.18.y/drivers/md/dm-cache-target.c' l='2278' u='r' c='parse_features'/>
<doc f='linux-4.18.y/drivers/md/dm-cache-target.c' l='343'>/*
	 * A degraded mode useful for various cache coherency situations
	 * (eg, rolling back snapshots).  Reads and writes always go to the
	 * origin.  If a write goes to a cached oblock, then the cache
	 * block is invalidated.
	 */</doc>
