<def f='linux-4.14.y/kernel/rcu/rcu_segcblist.h' l='50' ll='53' type='bool rcu_segcblist_empty(struct rcu_segcblist * rsclp)'/>
<use f='linux-4.14.y/kernel/rcu/rcu_segcblist.h' l='109' u='c' c='rcu_segcblist_tail'/>
<use f='linux-4.14.y/kernel/rcu/rcu_segcblist.c' l='81' u='c' c='rcu_segcblist_disable'/>
<doc f='linux-4.14.y/kernel/rcu/rcu_segcblist.h' l='37'>/*
 * Is the specified rcu_segcblist structure empty?
 *
 * But careful!  The fact that the -&gt;head field is NULL does not
 * necessarily imply that there are no callbacks associated with
 * this structure.  When callbacks are being invoked, they are
 * removed as a group.  If callback invocation must be preempted,
 * the remaining callbacks will be added back to the list.  Either
 * way, the counts are updated later.
 *
 * So it is often the case that rcu_segcblist_n_cbs() should be used
 * instead.
 */</doc>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='2725' u='c' c='rcu_do_batch'/>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='3069' u='c' c='__call_rcu'/>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='3454' u='c' c='rcu_cpu_has_callbacks'/>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='3685' u='c' c='rcu_init_percpu_data'/>
<def f='linux-4.14.y/kernel/rcu/rcu_segcblist.h' l='50' ll='53' type='bool rcu_segcblist_empty(struct rcu_segcblist * rsclp)'/>
<use f='linux-4.14.y/kernel/rcu/rcu_segcblist.h' l='109' u='c' c='rcu_segcblist_tail'/>
<use f='linux-4.14.y/kernel/rcu/rcu_segcblist.c' l='81' u='c' c='rcu_segcblist_disable'/>
<doc f='linux-4.14.y/kernel/rcu/rcu_segcblist.h' l='37'>/*
 * Is the specified rcu_segcblist structure empty?
 *
 * But careful!  The fact that the -&gt;head field is NULL does not
 * necessarily imply that there are no callbacks associated with
 * this structure.  When callbacks are being invoked, they are
 * removed as a group.  If callback invocation must be preempted,
 * the remaining callbacks will be added back to the list.  Either
 * way, the counts are updated later.
 *
 * So it is often the case that rcu_segcblist_n_cbs() should be used
 * instead.
 */</doc>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='2725' u='c' c='rcu_do_batch'/>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='3069' u='c' c='__call_rcu'/>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='3454' u='c' c='rcu_cpu_has_callbacks'/>
<use f='linux-4.14.y/kernel/rcu/tree.c' l='3685' u='c' c='rcu_init_percpu_data'/>
