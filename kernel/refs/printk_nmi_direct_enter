<dec f='linux-4.18.y/include/linux/printk.h' l='151' type='void printk_nmi_direct_enter()'/>
<def f='linux-4.18.y/kernel/printk/printk_safe.c' l='329' ll='333' type='void printk_nmi_direct_enter()'/>
<doc f='linux-4.18.y/kernel/printk/printk_safe.c' l='319'>/*
 * Marks a code that might produce many messages in NMI context
 * and the risk of losing them is more critical than eventual
 * reordering.
 *
 * It has effect only when called in NMI context. Then printk()
 * will try to store the messages into the main logbuf directly
 * and use the per-CPU buffers only as a fallback when the lock
 * is not available.
 */</doc>
<use f='linux-4.18.y/kernel/trace/trace.c' l='8293' u='c' c='ftrace_dump'/>
