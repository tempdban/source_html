<dec f='linux-4.14.y/include/linux/fs.h' l='2431' type='struct super_block * freeze_bdev(struct block_device * )'/>
<use f='linux-4.14.y/drivers/md/dm.c' l='2305' u='c' c='lock_fs'/>
<def f='linux-4.14.y/fs/block_dev.c' l='502' ll='539' type='struct super_block * freeze_bdev(struct block_device * bdev)'/>
<dec f='linux-4.14.y/fs/block_dev.c' l='540' type='struct super_block * freeze_bdev(struct block_device * )'/>
<use f='linux-4.14.y/fs/block_dev.c' l='540' c='freeze_bdev'/>
<use f='linux-4.14.y/fs/block_dev.c' l='540' u='a'/>
<use f='linux-4.14.y/fs/block_dev.c' l='540' u='a'/>
<doc f='linux-4.14.y/fs/block_dev.c' l='490'>/**
 * freeze_bdev  --  lock a filesystem and force it into a consistent state
 * @bdev:	blockdevice to lock
 *
 * If a superblock is found on this device, we take the s_umount semaphore
 * on it to make sure nobody unmounts until the snapshot creation is done.
 * The reference counter (bd_fsfreeze_count) guarantees that only the last
 * unfreeze process can unfreeze the frozen filesystem actually when multiple
 * freeze requests arrive simultaneously. It counts up in freeze_bdev() and
 * count down in thaw_bdev(). When it becomes 0, thaw_bdev() will unfreeze
 * actually.
 */</doc>
<use f='linux-4.14.y/fs/ext4/ioctl.c' l='488' u='c' c='ext4_shutdown'/>
