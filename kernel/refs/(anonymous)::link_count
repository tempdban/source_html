<dec f='linux-4.18.y/fs/ntfs/layout.h' l='340' type='le16'/>
<dec f='linux-4.18.y/fs/ntfs/layout.h' l='409' type='le16'/>
<offset>144</offset>
<doc f='linux-4.18.y/fs/ntfs/layout.h' l='340'>/* 18*/</doc>
<doc f='linux-4.18.y/fs/ntfs/layout.h' l='340'>/* Number of hard links, i.e. the number of
				   directory entries referencing this record.
				   NOTE: Only used in mft base records.
				   NOTE: When deleting a directory entry we
				   check the link_count and if it is 1 we
				   delete the file. Otherwise we delete the
				   FILE_NAME_ATTR being referenced by the
				   directory entry from the mft record and
				   decrement the link_count.
				   FIXME: Careful with Win32 + DOS names! */</doc>
<doc f='linux-4.18.y/fs/ntfs/layout.h' l='409'>/* 18*/</doc>
<doc f='linux-4.18.y/fs/ntfs/layout.h' l='409'>/* Number of hard links, i.e. the number of
				   directory entries referencing this record.
				   NOTE: Only used in mft base records.
				   NOTE: When deleting a directory entry we
				   check the link_count and if it is 1 we
				   delete the file. Otherwise we delete the
				   FILE_NAME_ATTR being referenced by the
				   directory entry from the mft record and
				   decrement the link_count.
				   FIXME: Careful with Win32 + DOS names! */</doc>
<use f='linux-4.18.y/fs/ntfs/inode.c' l='465' u='r' c='ntfs_is_extended_system_file'/>
<use f='linux-4.18.y/fs/ntfs/inode.c' l='607' u='r' c='ntfs_read_locked_inode'/>
<use f='linux-4.18.y/fs/ntfs/mft.c' l='2056' u='w' c='ntfs_mft_record_layout'/>
