<dec f='linux-4.14.y/include/linux/tty.h' l='486' type='int tty_throttle_safe(struct tty_struct * tty)'/>
<use f='linux-4.14.y/drivers/tty/n_tty.c' l='255' u='c' c='n_tty_check_throttle'/>
<def f='linux-4.14.y/drivers/tty/tty_ioctl.c' l='156' ll='173' type='int tty_throttle_safe(struct tty_struct * tty)'/>
<doc f='linux-4.14.y/drivers/tty/tty_ioctl.c' l='144'>/**
 *	tty_throttle_safe	-	flow control
 *	@tty: terminal
 *
 *	Similar to tty_throttle() but will only attempt throttle
 *	if tty-&gt;flow_change is TTY_THROTTLE_SAFE. Prevents an accidental
 *	throttle due to race conditions when throttling is conditional
 *	on factors evaluated prior to throttling.
 *
 *	Returns 0 if tty is throttled (or was already throttled)
 */</doc>
<dec f='linux-4.14.y/include/linux/tty.h' l='486' type='int tty_throttle_safe(struct tty_struct * tty)'/>
<use f='linux-4.14.y/drivers/tty/n_tty.c' l='255' u='c' c='n_tty_check_throttle'/>
<def f='linux-4.14.y/drivers/tty/tty_ioctl.c' l='156' ll='173' type='int tty_throttle_safe(struct tty_struct * tty)'/>
<doc f='linux-4.14.y/drivers/tty/tty_ioctl.c' l='144'>/**
 *	tty_throttle_safe	-	flow control
 *	@tty: terminal
 *
 *	Similar to tty_throttle() but will only attempt throttle
 *	if tty-&gt;flow_change is TTY_THROTTLE_SAFE. Prevents an accidental
 *	throttle due to race conditions when throttling is conditional
 *	on factors evaluated prior to throttling.
 *
 *	Returns 0 if tty is throttled (or was already throttled)
 */</doc>
