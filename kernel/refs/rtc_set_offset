<dec f='linux-4.18.y/include/linux/rtc.h' l='230' type='int rtc_set_offset(struct rtc_device * rtc, long offset)'/>
<def f='linux-4.18.y/drivers/rtc/interface.c' l='1127' ll='1143' type='int rtc_set_offset(struct rtc_device * rtc, long offset)'/>
<doc f='linux-4.18.y/drivers/rtc/interface.c' l='1110'>/**
 * rtc_set_offset - Adjusts the duration of the average second
 * @ rtc: rtc device to be used
 * @ offset: the offset in parts per billion
 *
 * Some rtc&apos;s allow an adjustment to the average duration of a second
 * to compensate for differences in the actual clock rate due to temperature,
 * the crystal, capacitor, etc.
 *
 * The adjustment applied is as follows:
 *   t = t0 * (1 + offset * 1e-9)
 * where t0 is the measured length of 1 RTC second with offset = 0
 *
 * Kernel interface to adjust an rtc clock offset.
 * Return 0 on success, or a negative number on error.
 * If the rtc offset is not setable (or not implemented), return -EINVAL
 */</doc>
<use f='linux-4.18.y/drivers/rtc/rtc-sysfs.c' l='245' u='c' c='offset_store'/>
