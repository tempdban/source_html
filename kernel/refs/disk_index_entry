<def f='linux-4.14.y/drivers/md/persistent-data/dm-space-map-common.h' l='32' ll='36'/>
<size>16</size>
<doc f='linux-4.14.y/drivers/md/persistent-data/dm-space-map-common.h' l='14'>/*
 * Low level disk format
 *
 * Bitmap btree
 * ------------
 *
 * Each value stored in the btree is an index_entry.  This points to a
 * block that is used as a bitmap.  Within the bitmap hold 2 bits per
 * entry, which represent UNUSED = 0, REF_COUNT = 1, REF_COUNT = 2 and
 * REF_COUNT = many.
 *
 * Refcount btree
 * --------------
 *
 * Any entry that has a ref count higher than 2 gets entered in the ref
 * count tree.  The leaf values for this tree is the 32-bit ref count.
 */</doc>
<mbr r='disk_index_entry::blocknr' o='0' t='__le64'/>
<mbr r='disk_index_entry::nr_free' o='64' t='__le32'/>
<mbr r='disk_index_entry::none_free_before' o='96' t='__le32'/>
<def f='linux-4.14.y/drivers/md/persistent-data/dm-space-map-common.h' l='32' ll='36'/>
<size>16</size>
<doc f='linux-4.14.y/drivers/md/persistent-data/dm-space-map-common.h' l='14'>/*
 * Low level disk format
 *
 * Bitmap btree
 * ------------
 *
 * Each value stored in the btree is an index_entry.  This points to a
 * block that is used as a bitmap.  Within the bitmap hold 2 bits per
 * entry, which represent UNUSED = 0, REF_COUNT = 1, REF_COUNT = 2 and
 * REF_COUNT = many.
 *
 * Refcount btree
 * --------------
 *
 * Any entry that has a ref count higher than 2 gets entered in the ref
 * count tree.  The leaf values for this tree is the 32-bit ref count.
 */</doc>
<mbr r='disk_index_entry::blocknr' o='0' t='__le64'/>
<mbr r='disk_index_entry::nr_free' o='64' t='__le32'/>
<mbr r='disk_index_entry::none_free_before' o='96' t='__le32'/>
