<dec f='linux-4.14.y/include/linux/selection.h' l='20' type='int set_selection(const struct tiocl_selection * sel, struct tty_struct * tty)'/>
<def f='linux-4.14.y/drivers/tty/vt/selection.c' l='156' ll='325' type='int set_selection(const struct tiocl_selection * sel, struct tty_struct * tty)'/>
<doc f='linux-4.14.y/drivers/tty/vt/selection.c' l='146'>/**
 *	set_selection		- 	set the current selection.
 *	@sel: user selection info
 *	@tty: the console tty
 *
 *	Invoked by the ioctl handle for the vt layer.
 *
 *	The entire selection process is managed under the console_lock. It&apos;s
 *	 a lot under the lock but its hardly a performance path
 */</doc>
<use f='linux-4.14.y/drivers/tty/vt/vt.c' l='2691' u='c' c='tioclinux'/>
<dec f='linux-4.14.y/include/linux/selection.h' l='20' type='int set_selection(const struct tiocl_selection * sel, struct tty_struct * tty)'/>
<def f='linux-4.14.y/drivers/tty/vt/selection.c' l='156' ll='325' type='int set_selection(const struct tiocl_selection * sel, struct tty_struct * tty)'/>
<doc f='linux-4.14.y/drivers/tty/vt/selection.c' l='146'>/**
 *	set_selection		- 	set the current selection.
 *	@sel: user selection info
 *	@tty: the console tty
 *
 *	Invoked by the ioctl handle for the vt layer.
 *
 *	The entire selection process is managed under the console_lock. It&apos;s
 *	 a lot under the lock but its hardly a performance path
 */</doc>
<use f='linux-4.14.y/drivers/tty/vt/vt.c' l='2691' u='c' c='tioclinux'/>
