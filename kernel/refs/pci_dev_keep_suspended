<dec f='linux-4.18.y/drivers/pci/pci.h' l='77' type='bool pci_dev_keep_suspended(struct pci_dev * dev)'/>
<def f='linux-4.18.y/drivers/pci/pci.c' l='2231' ll='2259' type='bool pci_dev_keep_suspended(struct pci_dev * pci_dev)'/>
<doc f='linux-4.18.y/drivers/pci/pci.c' l='2219'>/**
 * pci_dev_keep_suspended - Check if the device can stay in the suspended state.
 * @pci_dev: Device to check.
 *
 * Return &apos;true&apos; if the device is runtime-suspended, it doesn&apos;t have to be
 * reconfigured due to wakeup settings difference between system and runtime
 * suspend and the current power state of it is suitable for the upcoming
 * (system) transition.
 *
 * If the device is not configured for system wakeup, disable PME for it before
 * returning &apos;true&apos; to prevent it from waking up the system unnecessarily.
 */</doc>
