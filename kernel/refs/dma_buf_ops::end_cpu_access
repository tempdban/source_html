<dec f='linux-4.14.y/include/linux/dma-buf.h' l='208' type='int (*)(struct dma_buf *, enum dma_data_direction)'/>
<use f='linux-4.14.y/drivers/dma-buf/dma-buf.c' l='855' u='r' c='dma_buf_end_cpu_access'/>
<use f='linux-4.14.y/drivers/dma-buf/dma-buf.c' l='856' u='r' c='dma_buf_end_cpu_access'/>
<offset>384</offset>
<doc f='linux-4.14.y/include/linux/dma-buf.h' l='191'>/**
	 * @end_cpu_access:
	 *
	 * This is called from dma_buf_end_cpu_access() when the importer is
	 * done accessing the CPU. The exporter can use this to flush caches and
	 * unpin any resources pinned in @begin_cpu_access.
	 * The result of any dma_buf kmap calls after end_cpu_access is
	 * undefined.
	 *
	 * This callback is optional.
	 *
	 * Returns:
	 *
	 * 0 on success or a negative error code on failure. Can return
	 * -ERESTARTSYS or -EINTR when the call has been interrupted and needs
	 * to be restarted.
	 */</doc>
<dec f='linux-4.14.y/include/linux/dma-buf.h' l='208' type='int (*)(struct dma_buf *, enum dma_data_direction)'/>
<use f='linux-4.14.y/drivers/dma-buf/dma-buf.c' l='855' u='r' c='dma_buf_end_cpu_access'/>
<use f='linux-4.14.y/drivers/dma-buf/dma-buf.c' l='856' u='r' c='dma_buf_end_cpu_access'/>
<offset>384</offset>
<doc f='linux-4.14.y/include/linux/dma-buf.h' l='191'>/**
	 * @end_cpu_access:
	 *
	 * This is called from dma_buf_end_cpu_access() when the importer is
	 * done accessing the CPU. The exporter can use this to flush caches and
	 * unpin any resources pinned in @begin_cpu_access.
	 * The result of any dma_buf kmap calls after end_cpu_access is
	 * undefined.
	 *
	 * This callback is optional.
	 *
	 * Returns:
	 *
	 * 0 on success or a negative error code on failure. Can return
	 * -ERESTARTSYS or -EINTR when the call has been interrupted and needs
	 * to be restarted.
	 */</doc>
