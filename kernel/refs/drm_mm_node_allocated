<def f='linux-4.14.y/include/drm/drm_mm.h' l='222' ll='225' type='bool drm_mm_node_allocated(const struct drm_mm_node * node)'/>
<doc f='linux-4.14.y/include/drm/drm_mm.h' l='209'>/**
 * drm_mm_node_allocated - checks whether a node is allocated
 * @node: drm_mm_node to check
 *
 * Drivers are required to clear a node prior to using it with the
 * drm_mm range manager.
 *
 * Drivers should use this helper for proper encapsulation of drm_mm
 * internals.
 *
 * Returns:
 * True if the @node is allocated.
 */</doc>
<use f='linux-4.14.y/include/drm/drm_vma_manager.h' l='209' u='c' c='drm_vma_node_unmap'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_vma_manager.c' l='210' u='c' c='drm_vma_offset_add'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_vma_manager.c' l='241' u='c' c='drm_vma_offset_remove'/>
<def f='linux-4.14.y/include/drm/drm_mm.h' l='222' ll='225' type='bool drm_mm_node_allocated(const struct drm_mm_node * node)'/>
<doc f='linux-4.14.y/include/drm/drm_mm.h' l='209'>/**
 * drm_mm_node_allocated - checks whether a node is allocated
 * @node: drm_mm_node to check
 *
 * Drivers are required to clear a node prior to using it with the
 * drm_mm range manager.
 *
 * Drivers should use this helper for proper encapsulation of drm_mm
 * internals.
 *
 * Returns:
 * True if the @node is allocated.
 */</doc>
<use f='linux-4.14.y/include/drm/drm_vma_manager.h' l='209' u='c' c='drm_vma_node_unmap'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_vma_manager.c' l='210' u='c' c='drm_vma_offset_add'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_vma_manager.c' l='241' u='c' c='drm_vma_offset_remove'/>
