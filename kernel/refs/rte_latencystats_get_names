<dec f='linux-4.18.y/lib/librte_latencystats/rte_latencystats.h' l='99' type='int rte_latencystats_get_names(struct rte_metric_name * names, uint16_t size)'/>
<doc f='linux-4.18.y/lib/librte_latencystats/rte_latencystats.h' l='83'>/**
 * Retrieve names of latency statistics
 *
 * @param names
 *  Block of memory to insert names into. Must be at least size in capacity.
 *  If set to NULL, function returns required capacity.
 * @param size
 *  Capacity of latency stats names (number of names).
 * @return
 *   - positive value lower or equal to size: success. The return value
 *     is the number of entries filled in the stats table.
 *   - positive value higher than size: error, the given statistics table
 *     is too small. The return value corresponds to the size that should
 *     be given to succeed. The entries in the table are not valid and
 *     shall not be used by the caller.
 */</doc>
<def f='linux-4.18.y/lib/librte_latencystats/rte_latencystats.c' l='300' ll='313' type='int rte_latencystats_get_names(struct rte_metric_name * names, uint16_t size)'/>
