<def f='linux-4.14.y/arch/x86/include/asm/apic.h' l='78' ll='81' type='bool apic_from_smp_config()'/>
<doc f='linux-4.14.y/arch/x86/include/asm/apic.h' l='70'>/*
 * With 82489DX we can&apos;t rely on apic feature bit
 * retrieved via cpuid but still have to deal with
 * such an apic chip so we assume that SMP configuration
 * is found from MP table (64bit case uses ACPI mostly
 * which set smp presence flag as well so we are safe
 * to use this helper too).
 */</doc>
<use f='linux-4.14.y/arch/x86/kernel/apic/apic.c' l='1194' u='c' c='lapic_shutdown'/>
<use f='linux-4.14.y/arch/x86/kernel/apic/io_apic.c' l='1436' u='c' c='native_disable_io_apic'/>
<use f='linux-4.14.y/arch/x86/kernel/apic/vector.c' l='988' u='c' c='print_ICs'/>
<use f='linux-4.14.y/drivers/iommu/irq_remapping.c' l='39' u='c' c='irq_remapping_disable_io_apic'/>
<def f='linux-4.14.y/arch/x86/include/asm/apic.h' l='78' ll='81' type='bool apic_from_smp_config()'/>
<doc f='linux-4.14.y/arch/x86/include/asm/apic.h' l='70'>/*
 * With 82489DX we can&apos;t rely on apic feature bit
 * retrieved via cpuid but still have to deal with
 * such an apic chip so we assume that SMP configuration
 * is found from MP table (64bit case uses ACPI mostly
 * which set smp presence flag as well so we are safe
 * to use this helper too).
 */</doc>
<use f='linux-4.14.y/arch/x86/kernel/apic/apic.c' l='1194' u='c' c='lapic_shutdown'/>
<use f='linux-4.14.y/arch/x86/kernel/apic/io_apic.c' l='1436' u='c' c='native_disable_io_apic'/>
<use f='linux-4.14.y/arch/x86/kernel/apic/vector.c' l='988' u='c' c='print_ICs'/>
<use f='linux-4.14.y/drivers/iommu/irq_remapping.c' l='39' u='c' c='irq_remapping_disable_io_apic'/>
