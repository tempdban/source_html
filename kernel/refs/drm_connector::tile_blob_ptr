<dec f='linux-4.18.y/include/drm/drm_connector.h' l='893' type='struct drm_property_blob *'/>
<offset>6080</offset>
<doc f='linux-4.18.y/include/drm/drm_connector.h' l='883'>/**
	 * @tile_blob_ptr:
	 *
	 * DRM blob property data for the tile property (used mostly by DP MST).
	 * This is meant for screens which are driven through separate display
	 * pipelines represented by &amp;drm_crtc, which might not be running with
	 * genlocked clocks. For tiled panels which are genlocked, like
	 * dual-link LVDS or dual-link DSI, the driver should try to not expose
	 * the tiling and virtualize both &amp;drm_crtc and &amp;drm_plane if needed.
	 */</doc>
<use f='linux-4.18.y/drivers/gpu/drm/drm_connector.c' l='1335' u='a' c='drm_mode_connector_set_tile_property'/>
<use f='linux-4.18.y/drivers/gpu/drm/drm_connector.c' l='1350' u='a' c='drm_mode_connector_set_tile_property'/>
