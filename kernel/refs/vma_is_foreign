<def f='linux-4.14.y/arch/x86/include/asm/mmu_context.h' l='314' ll='327' type='bool vma_is_foreign(struct vm_area_struct * vma)'/>
<use f='linux-4.14.y/arch/x86/include/asm/mmu_context.h' l='336' u='c' c='arch_vma_access_permitted'/>
<doc f='linux-4.14.y/arch/x86/include/asm/mmu_context.h' l='305'>/*
 * We only want to enforce protection keys on the current process
 * because we effectively have no access to PKRU for other
 * processes or any way to tell *which * PKRU in a threaded
 * process we could use.
 *
 * So do not enforce things if the VMA is not from the current
 * mm, or if we are in a kernel thread.
 */</doc>
