<dec f='linux-4.14.y/include/drm/drm_mode_config.h' l='444' type='int'/>
<offset>2368</offset>
<doc f='linux-4.14.y/include/drm/drm_mode_config.h' l='432'>/**
	 * @num_overlay_plane:
	 *
	 * Number of overlay planes on this device, excluding primary and cursor
	 * planes.
	 *
	 * Track number of overlay planes separately from number of total
	 * planes.  By default we only advertise overlay planes to userspace; if
	 * userspace sets the &quot;universal plane&quot; capability bit, we&apos;ll go ahead
	 * and expose all planes. This is invariant over the lifetime of a
	 * device and hence doesn&apos;t need any locks.
	 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_mode_config.c' l='388' u='w' c='drm_mode_config_init'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_plane.c' l='245' u='w' c='drm_universal_plane_init'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_plane.c' l='357' u='w' c='drm_plane_cleanup'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_plane.c' l='476' u='r' c='drm_mode_getplane_res'/>
<dec f='linux-4.14.y/include/drm/drm_mode_config.h' l='444' type='int'/>
<offset>2368</offset>
<doc f='linux-4.14.y/include/drm/drm_mode_config.h' l='432'>/**
	 * @num_overlay_plane:
	 *
	 * Number of overlay planes on this device, excluding primary and cursor
	 * planes.
	 *
	 * Track number of overlay planes separately from number of total
	 * planes.  By default we only advertise overlay planes to userspace; if
	 * userspace sets the &quot;universal plane&quot; capability bit, we&apos;ll go ahead
	 * and expose all planes. This is invariant over the lifetime of a
	 * device and hence doesn&apos;t need any locks.
	 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_mode_config.c' l='388' u='w' c='drm_mode_config_init'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_plane.c' l='245' u='w' c='drm_universal_plane_init'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_plane.c' l='357' u='w' c='drm_plane_cleanup'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_plane.c' l='476' u='r' c='drm_mode_getplane_res'/>
