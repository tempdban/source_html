<dec f='linux-4.14.y/include/drm/drm_mm.h' l='111' type='3'/>
<doc f='linux-4.14.y/include/drm/drm_mm.h' l='101'>/**
	 * @DRM_MM_INSERT_EVICT:
	 *
	 * Search for the most recently evicted hole (within the search range)
	 * that fits the desired node. This is appropriate for use immediately
	 * after performing an eviction scan (see drm_mm_scan_init()) and
	 * removing the selected nodes to form a hole.
	 *
	 * Allocates the node from the bottom of the found hole.
	 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_mm.c' l='342' c='first_hole'/>
<use f='linux-4.14.y/drivers/gpu/drm/drm_mm.c' l='365' c='next_hole'/>
