<def f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc.c' l='92' ll='96'/>
<size>12</size>
<doc f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc.c' l='86'>/**
 * Limits for the pool. They are handled without locks because only place where
 * they may change is in sysfs store. They won&apos;t have immediate effect anyway
 * so forcing serialization to access them is pointless.
 */</doc>
<def f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc_dma.c' l='144' ll='148'/>
<size>12</size>
<doc f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc_dma.c' l='138'>/*
 * Limits for the pool. They are handled without locks because only place where
 * they may change is in sysfs store. They won&apos;t have immediate effect anyway
 * so forcing serialization to access them is pointless.
 */</doc>
<def f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc.c' l='92' ll='96'/>
<size>12</size>
<doc f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc.c' l='86'>/**
 * Limits for the pool. They are handled without locks because only place where
 * they may change is in sysfs store. They won&apos;t have immediate effect anyway
 * so forcing serialization to access them is pointless.
 */</doc>
<def f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc_dma.c' l='144' ll='148'/>
<size>12</size>
<doc f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_page_alloc_dma.c' l='138'>/*
 * Limits for the pool. They are handled without locks because only place where
 * they may change is in sysfs store. They won&apos;t have immediate effect anyway
 * so forcing serialization to access them is pointless.
 */</doc>
