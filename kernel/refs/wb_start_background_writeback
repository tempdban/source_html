<dec f='linux-4.14.y/include/linux/backing-dev.h' l='44' type='void wb_start_background_writeback(struct bdi_writeback * wb)'/>
<def f='linux-4.14.y/fs/fs-writeback.c' l='976' ll='984' type='void wb_start_background_writeback(struct bdi_writeback * wb)'/>
<doc f='linux-4.14.y/fs/fs-writeback.c' l='966'>/**
 * wb_start_background_writeback - start background writeback
 * @wb: bdi_writback to write from
 *
 * Description:
 *   This makes sure WB_SYNC_NONE background writeback happens. When
 *   this function returns, it is only guaranteed that for given wb
 *   some IO is happening if we are over background dirty threshold.
 *   Caller need not hold sb s_umount semaphore.
 */</doc>
<use f='linux-4.14.y/mm/page-writeback.c' l='1670' u='c' c='balance_dirty_pages'/>
<use f='linux-4.14.y/mm/page-writeback.c' l='1830' u='c' c='balance_dirty_pages'/>
