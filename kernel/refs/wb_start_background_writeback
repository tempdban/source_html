<dec f='linux-4.18.y/include/linux/backing-dev.h' l='43' type='void wb_start_background_writeback(struct bdi_writeback * wb)'/>
<def f='linux-4.18.y/fs/fs-writeback.c' l='979' ll='987' type='void wb_start_background_writeback(struct bdi_writeback * wb)'/>
<doc f='linux-4.18.y/fs/fs-writeback.c' l='969'>/**
 * wb_start_background_writeback - start background writeback
 * @wb: bdi_writback to write from
 *
 * Description:
 *   This makes sure WB_SYNC_NONE background writeback happens. When
 *   this function returns, it is only guaranteed that for given wb
 *   some IO is happening if we are over background dirty threshold.
 *   Caller need not hold sb s_umount semaphore.
 */</doc>
<use f='linux-4.18.y/mm/page-writeback.c' l='1668' u='c' c='balance_dirty_pages'/>
<use f='linux-4.18.y/mm/page-writeback.c' l='1828' u='c' c='balance_dirty_pages'/>
