<def f='linux-4.18.y/include/linux/sbitmap.h' l='441' ll='451' type='int sbitmap_queue_get_shallow(struct sbitmap_queue * sbq, unsigned int * cpu, unsigned int shallow_depth)'/>
<doc f='linux-4.18.y/include/linux/sbitmap.h' l='427'>/**
 * sbitmap_queue_get_shallow() - Try to allocate a free bit from a &amp;struct
 * sbitmap_queue, limiting the depth used from each word.
 * @sbq: Bitmap queue to allocate from.
 * @cpu: Output parameter; will contain the CPU we ran on (e.g., to be passed to
 *       sbitmap_queue_clear()).
 * @shallow_depth: The maximum number of bits to allocate from a single word.
 * See sbitmap_get_shallow().
 *
 * If you call this, make sure to call sbitmap_queue_min_shallow_depth() after
 * initializing @sbq.
 *
 * Return: Non-negative allocated bit number if successful, -1 otherwise.
 */</doc>
