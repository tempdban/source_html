<def f='linux-4.18.y/include/linux/rtc.h' l='247' ll='269' type='bool rtc_tv_nsec_ok(s64 set_offset_nsec, struct timespec64 * to_set, const struct timespec64 * now)'/>
<doc f='linux-4.18.y/include/linux/rtc.h' l='238'>/* Determine if we can call to driver to set the time. Drivers can only be
 * called to set a second aligned time value, and the field set_offset_nsec
 * specifies how far away from the second aligned time to call the driver.
 *
 * This also computes &apos;to_set&apos; which is the time we are trying to set, and has
 * a zero in tv_nsecs, such that:
 *    to_set - set_delay_nsec == now +/- FUZZ
 *
 */</doc>
<use f='linux-4.18.y/drivers/rtc/systohc.c' l='53' u='c' c='rtc_set_ntp_time'/>
<use f='linux-4.18.y/kernel/time/ntp.c' l='595' u='c' c='sync_cmos_clock'/>
