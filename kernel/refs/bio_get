<def f='linux-4.18.y/include/linux/bio.h' l='249' ll='254' type='void bio_get(struct bio * bio)'/>
<use f='linux-4.18.y/block/bio.c' l='1441' u='c' c='bio_map_user_iov'/>
<doc f='linux-4.18.y/include/linux/bio.h' l='235'>/*
 * get a reference to a bio, so it won&apos;t disappear. the intended use is
 * something like:
 *
 * bio_get(bio);
 * submit_bio(rw, bio);
 * if (bio-&gt;bi_flags ...)
 *	do_something
 * bio_put(bio);
 *
 * without the bio_get(), it could potentially complete I/O before submit_bio
 * returns. and then bio would be freed memory when if (bio-&gt;bi_flags ...)
 * runs
 */</doc>
<use f='linux-4.18.y/block/blk-map.c' l='88' u='c' c='__blk_rq_map_user_iov'/>
<use f='linux-4.18.y/fs/block_dev.c' l='341' u='c' c='__blkdev_direct_IO'/>
<use f='linux-4.18.y/mm/page_io.c' l='404' u='c' c='swap_readpage'/>
