<def f='linux-4.14.y/include/linux/crash_dump.h' l='72' ll='75' type='int is_vmcore_usable()'/>
<doc f='linux-4.14.y/include/linux/crash_dump.h' l='64'>/* is_vmcore_usable() checks if the kernel is booting after a panic and
 * the vmcore region is usable.
 *
 * This makes use of the fact that due to alignment -2ULL is not
 * a valid pointer, much in the vain of IS_ERR(), except
 * dealing directly with an unsigned long long rather than a pointer.
 */</doc>
<use f='linux-4.14.y/fs/proc/vmcore.c' l='1161' u='c' c='vmcore_init'/>
