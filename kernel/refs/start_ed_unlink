<def f='linux-4.18.y/drivers/usb/host/ohci-q.c' l='484' ll='507' type='void start_ed_unlink(struct ohci_hcd * ohci, struct ed * ed)'/>
<use f='linux-4.18.y/drivers/usb/host/ohci-q.c' l='1168' u='c' c='takeback_td'/>
<use f='linux-4.18.y/drivers/usb/host/ohci-hcd.c' l='331' u='c' c='ohci_urb_dequeue'/>
<doc f='linux-4.18.y/drivers/usb/host/ohci-q.c' l='478'>/* request unlinking of an endpoint from an operational HC.
 * put the ep on the rm_list
 * real work is done at the next start frame (SF) hardware interrupt
 * caller guarantees HCD is running, so hardware access is safe,
 * and that ed-&gt;state is ED_OPER
 */</doc>
