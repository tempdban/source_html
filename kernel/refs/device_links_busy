<dec f='linux-4.14.y/drivers/base/base.h' l='162' type='bool device_links_busy(struct device * dev)'/>
<def f='linux-4.14.y/drivers/base/core.c' l='516' ll='539' type='bool device_links_busy(struct device * dev)'/>
<doc f='linux-4.14.y/drivers/base/core.c' l='502'>/**
 * device_links_busy - Check if there are any busy links to consumers.
 * @dev: Device to check.
 *
 * Check each consumer of the device and return &apos;true&apos; if its link&apos;s status
 * is one of &quot;consumer probe&quot; or &quot;active&quot; (meaning that the given consumer is
 * probing right now or its driver is present).  Otherwise, change the link
 * state to &quot;supplier unbind&quot; to prevent the consumer from being probed
 * successfully going forward.
 *
 * Return &apos;false&apos; if there are no probing or active consumers.
 *
 * Links with the DL_FLAG_STATELESS flag set are ignored.
 */</doc>
<use f='linux-4.14.y/drivers/base/dd.c' l='819' u='c' c='__device_release_driver'/>
<dec f='linux-4.14.y/drivers/base/base.h' l='162' type='bool device_links_busy(struct device * dev)'/>
<def f='linux-4.14.y/drivers/base/core.c' l='516' ll='539' type='bool device_links_busy(struct device * dev)'/>
<doc f='linux-4.14.y/drivers/base/core.c' l='502'>/**
 * device_links_busy - Check if there are any busy links to consumers.
 * @dev: Device to check.
 *
 * Check each consumer of the device and return &apos;true&apos; if its link&apos;s status
 * is one of &quot;consumer probe&quot; or &quot;active&quot; (meaning that the given consumer is
 * probing right now or its driver is present).  Otherwise, change the link
 * state to &quot;supplier unbind&quot; to prevent the consumer from being probed
 * successfully going forward.
 *
 * Return &apos;false&apos; if there are no probing or active consumers.
 *
 * Links with the DL_FLAG_STATELESS flag set are ignored.
 */</doc>
<use f='linux-4.14.y/drivers/base/dd.c' l='819' u='c' c='__device_release_driver'/>
