<dec f='linux-4.18.y/arch/x86/include/asm/insn-eval.h' l='21' type='int insn_get_code_seg_params(struct pt_regs * regs)'/>
<use f='linux-4.18.y/arch/x86/kernel/umip.c' l='364' u='c' c='fixup_umip_exception'/>
<def f='linux-4.18.y/arch/x86/lib/insn-eval.c' l='740' ll='786' type='int insn_get_code_seg_params(struct pt_regs * regs)'/>
<doc f='linux-4.18.y/arch/x86/lib/insn-eval.c' l='724'>/**
 * insn_get_code_seg_params() - Obtain code segment parameters
 * @regs:	Structure with register values as seen when entering kernel mode
 *
 * Obtain address and operand sizes of the code segment. It is obtained from the
 * selector contained in the CS register in regs. In protected mode, the default
 * address is determined by inspecting the L and D bits of the segment
 * descriptor. In virtual-8086 mode, the default is always two bytes for both
 * address and operand sizes.
 *
 * Returns:
 *
 * An int containing ORed-in default parameters on success.
 *
 * -EINVAL on error.
 */</doc>
