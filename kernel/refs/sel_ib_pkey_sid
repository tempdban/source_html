<dec f='linux-4.18.y/security/selinux/include/ibpkey.h' l='29' type='int sel_ib_pkey_sid(u64 subnet_prefix, u16 pkey, u32 * sid)'/>
<def f='linux-4.18.y/security/selinux/ibpkey.c' l='190' ll='204' type='int sel_ib_pkey_sid(u64 subnet_prefix, u16 pkey_num, u32 * sid)'/>
<doc f='linux-4.18.y/security/selinux/ibpkey.c' l='177'>/**
 * sel_ib_pkey_sid - Lookup the SID of a PKEY
 * @subnet_prefix: subnet_prefix
 * @pkey_num: pkey number
 * @sid: pkey SID
 *
 * Description:
 * This function determines the SID of a PKEY using the fastest method
 * possible.  First the pkey table is queried, but if an entry can&apos;t be found
 * then the policy is queried and the result is added to the table to speedup
 * future queries.  Returns zero on success, negative values on failure.
 *
 */</doc>
