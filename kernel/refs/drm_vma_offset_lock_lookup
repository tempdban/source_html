<def f='linux-4.14.y/include/drm/drm_vma_manager.h' l='108' ll='111' type='void drm_vma_offset_lock_lookup(struct drm_vma_offset_manager * mgr)'/>
<doc f='linux-4.14.y/include/drm/drm_vma_manager.h' l='92'>/**
 * drm_vma_offset_lock_lookup() - Lock lookup for extended private use
 * @mgr: Manager object
 *
 * Lock VMA manager for extended lookups. Only locked VMA function calls
 * are allowed while holding this lock. All other contexts are blocked from VMA
 * until the lock is released via drm_vma_offset_unlock_lookup().
 *
 * Use this if you need to take a reference to the objects returned by
 * drm_vma_offset_lookup_locked() before releasing this lock again.
 *
 * This lock must not be used for anything else than extended lookups. You must
 * not call any other VMA helpers while holding this lock.
 *
 * Note: You&apos;re in atomic-context while holding this lock!
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_gem.c' l='1009' u='c' c='drm_gem_mmap'/>
<use f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_bo_vm.c' l='391' u='c' c='ttm_bo_vm_lookup'/>
<def f='linux-4.14.y/include/drm/drm_vma_manager.h' l='108' ll='111' type='void drm_vma_offset_lock_lookup(struct drm_vma_offset_manager * mgr)'/>
<doc f='linux-4.14.y/include/drm/drm_vma_manager.h' l='92'>/**
 * drm_vma_offset_lock_lookup() - Lock lookup for extended private use
 * @mgr: Manager object
 *
 * Lock VMA manager for extended lookups. Only locked VMA function calls
 * are allowed while holding this lock. All other contexts are blocked from VMA
 * until the lock is released via drm_vma_offset_unlock_lookup().
 *
 * Use this if you need to take a reference to the objects returned by
 * drm_vma_offset_lookup_locked() before releasing this lock again.
 *
 * This lock must not be used for anything else than extended lookups. You must
 * not call any other VMA helpers while holding this lock.
 *
 * Note: You&apos;re in atomic-context while holding this lock!
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_gem.c' l='1009' u='c' c='drm_gem_mmap'/>
<use f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_bo_vm.c' l='391' u='c' c='ttm_bo_vm_lookup'/>
