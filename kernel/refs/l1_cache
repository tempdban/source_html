<def f='linux-4.14.y/arch/x86/kernel/cpu/intel_cacheinfo.c' l='171' ll='179'/>
<size>4</size>
<doc f='linux-4.14.y/arch/x86/kernel/cpu/intel_cacheinfo.c' l='165'>/* AMD doesn&apos;t have CPUID4. Emulate it here to report the same
   information to the user.  This makes some assumptions about the machine:
   L2 not shared, no SMT etc. that is currently true on AMD CPUs.

   In theory the TLBs could be reported as fake type (they are in &quot;dummy&quot;).
   Maybe later */</doc>
<def f='linux-4.14.y/arch/x86/kernel/cpu/intel_cacheinfo.c' l='171' ll='179'/>
<size>4</size>
<doc f='linux-4.14.y/arch/x86/kernel/cpu/intel_cacheinfo.c' l='165'>/* AMD doesn&apos;t have CPUID4. Emulate it here to report the same
   information to the user.  This makes some assumptions about the machine:
   L2 not shared, no SMT etc. that is currently true on AMD CPUs.

   In theory the TLBs could be reported as fake type (they are in &quot;dummy&quot;).
   Maybe later */</doc>
