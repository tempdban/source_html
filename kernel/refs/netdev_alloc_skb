<def f='linux-4.14.y/include/linux/skbuff.h' l='2603' ll='2607' type='struct sk_buff * netdev_alloc_skb(struct net_device * dev, unsigned int length)'/>
<use f='linux-4.14.y/include/linux/skbuff.h' l='2619' u='c' c='dev_alloc_skb'/>
<doc f='linux-4.14.y/include/linux/skbuff.h' l='2590'>/**
 *	netdev_alloc_skb - allocate an skbuff for rx on a specific device
 *	@dev: network device to receive on
 *	@length: length to allocate
 *
 *	Allocate a new &amp;sk_buff and assign it a usage count of one. The
 *	buffer has unspecified headroom built in. Users should allocate
 *	the headroom they think they need without accounting for the
 *	built in space. The built in space is used for optimisations.
 *
 *	%NULL is returned if there is no free memory. Although this function
 *	allocates memory it can be called from an interrupt.
 */</doc>
<use f='linux-4.14.y/drivers/net/ethernet/intel/e100.c' l='2397' u='c' c='e100_loopback_test'/>
<def f='linux-4.14.y/include/linux/skbuff.h' l='2603' ll='2607' type='struct sk_buff * netdev_alloc_skb(struct net_device * dev, unsigned int length)'/>
<use f='linux-4.14.y/include/linux/skbuff.h' l='2619' u='c' c='dev_alloc_skb'/>
<doc f='linux-4.14.y/include/linux/skbuff.h' l='2590'>/**
 *	netdev_alloc_skb - allocate an skbuff for rx on a specific device
 *	@dev: network device to receive on
 *	@length: length to allocate
 *
 *	Allocate a new &amp;sk_buff and assign it a usage count of one. The
 *	buffer has unspecified headroom built in. Users should allocate
 *	the headroom they think they need without accounting for the
 *	built in space. The built in space is used for optimisations.
 *
 *	%NULL is returned if there is no free memory. Although this function
 *	allocates memory it can be called from an interrupt.
 */</doc>
<use f='linux-4.14.y/drivers/net/ethernet/intel/e100.c' l='2397' u='c' c='e100_loopback_test'/>
