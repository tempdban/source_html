<dec f='linux-4.14.y/include/linux/uprobes.h' l='137' type='void uprobe_notify_resume(struct pt_regs * regs)'/>
<use f='linux-4.14.y/arch/x86/entry/common.c' l='155' u='c' c='exit_to_usermode_loop'/>
<def f='linux-4.14.y/kernel/events/uprobes.c' l='1980' ll='1991' type='void uprobe_notify_resume(struct pt_regs * regs)'/>
<doc f='linux-4.14.y/kernel/events/uprobes.c' l='1969'>/*
 * On breakpoint hit, breakpoint notifier sets the TIF_UPROBE flag and
 * allows the thread to return from interrupt. After that handle_swbp()
 * sets utask-&gt;active_uprobe.
 *
 * On singlestep exception, singlestep notifier sets the TIF_UPROBE flag
 * and allows the thread to return from interrupt.
 *
 * While returning to userspace, thread notices the TIF_UPROBE flag and calls
 * uprobe_notify_resume().
 */</doc>
<dec f='linux-4.14.y/include/linux/uprobes.h' l='137' type='void uprobe_notify_resume(struct pt_regs * regs)'/>
<use f='linux-4.14.y/arch/x86/entry/common.c' l='155' u='c' c='exit_to_usermode_loop'/>
<def f='linux-4.14.y/kernel/events/uprobes.c' l='1980' ll='1991' type='void uprobe_notify_resume(struct pt_regs * regs)'/>
<doc f='linux-4.14.y/kernel/events/uprobes.c' l='1969'>/*
 * On breakpoint hit, breakpoint notifier sets the TIF_UPROBE flag and
 * allows the thread to return from interrupt. After that handle_swbp()
 * sets utask-&gt;active_uprobe.
 *
 * On singlestep exception, singlestep notifier sets the TIF_UPROBE flag
 * and allows the thread to return from interrupt.
 *
 * While returning to userspace, thread notices the TIF_UPROBE flag and calls
 * uprobe_notify_resume().
 */</doc>
