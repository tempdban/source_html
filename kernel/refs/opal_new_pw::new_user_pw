<dec f='linux-4.14.y/include/uapi/linux/sed-opal.h' l='98' type='struct opal_session_info'/>
<offset>2176</offset>
<doc f='linux-4.14.y/include/uapi/linux/sed-opal.h' l='89'>/* When we&apos;re not operating in sum, and we first set
	 * passwords we need to set them via ADMIN authority.
	 * After passwords are changed, we can set them via,
	 * User authorities.
	 * Because of this restriction we need to know about
	 * Two different users. One in &apos;session&apos; which we will use
	 * to start the session and new_userr_pw as the user we&apos;re
	 * chaning the pw for.
	 */</doc>
<dec f='linux-4.14.y/include/uapi/linux/sed-opal.h' l='98' type='struct opal_session_info'/>
<offset>2176</offset>
<doc f='linux-4.14.y/include/uapi/linux/sed-opal.h' l='89'>/* When we&apos;re not operating in sum, and we first set
	 * passwords we need to set them via ADMIN authority.
	 * After passwords are changed, we can set them via,
	 * User authorities.
	 * Because of this restriction we need to know about
	 * Two different users. One in &apos;session&apos; which we will use
	 * to start the session and new_userr_pw as the user we&apos;re
	 * chaning the pw for.
	 */</doc>
