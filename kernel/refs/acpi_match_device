<dec f='linux-4.14.y/include/linux/acpi.h' l='587' type='const struct acpi_device_id * acpi_match_device(const struct acpi_device_id * ids, const struct device * dev)'/>
<def f='linux-4.14.y/drivers/acpi/bus.c' l='814' ll='818' type='const struct acpi_device_id * acpi_match_device(const struct acpi_device_id * ids, const struct device * dev)'/>
<dec f='linux-4.14.y/drivers/acpi/bus.c' l='819' type='const struct acpi_device_id * acpi_match_device(const struct acpi_device_id * , const struct device * )'/>
<use f='linux-4.14.y/drivers/acpi/bus.c' l='819' c='acpi_match_device'/>
<use f='linux-4.14.y/drivers/acpi/bus.c' l='819' u='a'/>
<use f='linux-4.14.y/drivers/acpi/bus.c' l='819' u='a'/>
<doc f='linux-4.14.y/drivers/acpi/bus.c' l='803'>/**
 * acpi_match_device - Match a struct device against a given list of ACPI IDs
 * @ids: Array of struct acpi_device_id object to match against.
 * @dev: The device structure to match.
 *
 * Check if @dev has a valid ACPI handle and if there is a struct acpi_device
 * object for that handle and use that object to match against a given list of
 * device IDs.
 *
 * Return a pointer to the first matching ID on success or %NULL on failure.
 */</doc>
<use f='linux-4.14.y/drivers/i2c/i2c-core-acpi.c' l='240' u='c' c='i2c_acpi_match_device'/>
<dec f='linux-4.14.y/include/linux/acpi.h' l='587' type='const struct acpi_device_id * acpi_match_device(const struct acpi_device_id * ids, const struct device * dev)'/>
<def f='linux-4.14.y/drivers/acpi/bus.c' l='814' ll='818' type='const struct acpi_device_id * acpi_match_device(const struct acpi_device_id * ids, const struct device * dev)'/>
<dec f='linux-4.14.y/drivers/acpi/bus.c' l='819' type='const struct acpi_device_id * acpi_match_device(const struct acpi_device_id * , const struct device * )'/>
<use f='linux-4.14.y/drivers/acpi/bus.c' l='819' c='acpi_match_device'/>
<use f='linux-4.14.y/drivers/acpi/bus.c' l='819' u='a'/>
<use f='linux-4.14.y/drivers/acpi/bus.c' l='819' u='a'/>
<doc f='linux-4.14.y/drivers/acpi/bus.c' l='803'>/**
 * acpi_match_device - Match a struct device against a given list of ACPI IDs
 * @ids: Array of struct acpi_device_id object to match against.
 * @dev: The device structure to match.
 *
 * Check if @dev has a valid ACPI handle and if there is a struct acpi_device
 * object for that handle and use that object to match against a given list of
 * device IDs.
 *
 * Return a pointer to the first matching ID on success or %NULL on failure.
 */</doc>
<use f='linux-4.14.y/drivers/i2c/i2c-core-acpi.c' l='240' u='c' c='i2c_acpi_match_device'/>
