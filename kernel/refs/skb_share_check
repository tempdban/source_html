<def f='linux-4.14.y/include/linux/skbuff.h' l='1513' ll='1526' type='struct sk_buff * skb_share_check(struct sk_buff * skb, gfp_t pri)'/>
<doc f='linux-4.14.y/include/linux/skbuff.h' l='1500'>/**
 *	skb_share_check - check if buffer is shared and if so clone it
 *	@skb: buffer to check
 *	@pri: priority for memory allocation
 *
 *	If the buffer is shared the buffer is cloned and the old copy
 *	drops a reference. A new clone with a single reference is returned.
 *	If the buffer is not shared the original buffer is returned. When
 *	being called from interrupt status or with spinlocks held pri must
 *	be GFP_ATOMIC.
 *
 *	NULL is returned on a memory allocation failure.
 */</doc>
<use f='linux-4.14.y/net/core/skbuff.c' l='4969' u='c' c='skb_vlan_untag'/>
<use f='linux-4.14.y/net/ipv4/arp.c' l='951' u='c' c='arp_rcv'/>
<use f='linux-4.14.y/net/ipv4/ip_fragment.c' l='716' u='c' c='ip_check_defrag'/>
<use f='linux-4.14.y/net/ipv4/ip_input.c' l='428' u='c' c='ip_rcv'/>
<use f='linux-4.14.y/net/packet/af_packet.c' l='1857' u='c' c='packet_rcv_spkt'/>
<def f='linux-4.14.y/include/linux/skbuff.h' l='1513' ll='1526' type='struct sk_buff * skb_share_check(struct sk_buff * skb, gfp_t pri)'/>
<doc f='linux-4.14.y/include/linux/skbuff.h' l='1500'>/**
 *	skb_share_check - check if buffer is shared and if so clone it
 *	@skb: buffer to check
 *	@pri: priority for memory allocation
 *
 *	If the buffer is shared the buffer is cloned and the old copy
 *	drops a reference. A new clone with a single reference is returned.
 *	If the buffer is not shared the original buffer is returned. When
 *	being called from interrupt status or with spinlocks held pri must
 *	be GFP_ATOMIC.
 *
 *	NULL is returned on a memory allocation failure.
 */</doc>
<use f='linux-4.14.y/net/core/skbuff.c' l='4969' u='c' c='skb_vlan_untag'/>
<use f='linux-4.14.y/net/ipv4/arp.c' l='951' u='c' c='arp_rcv'/>
<use f='linux-4.14.y/net/ipv4/ip_fragment.c' l='716' u='c' c='ip_check_defrag'/>
<use f='linux-4.14.y/net/ipv4/ip_input.c' l='428' u='c' c='ip_rcv'/>
<use f='linux-4.14.y/net/packet/af_packet.c' l='1857' u='c' c='packet_rcv_spkt'/>
