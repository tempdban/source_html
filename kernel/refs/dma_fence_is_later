<def f='linux-4.14.y/include/linux/dma-fence.h' l='361' ll='368' type='bool dma_fence_is_later(struct dma_fence * f1, struct dma_fence * f2)'/>
<use f='linux-4.14.y/include/linux/dma-fence.h' l='390' u='c' c='dma_fence_later'/>
<doc f='linux-4.14.y/include/linux/dma-fence.h' l='353'>/**
 * dma_fence_is_later - return if f1 is chronologically later than f2
 * @f1:	[in]	the first fence from the same context
 * @f2:	[in]	the second fence from the same context
 *
 * Returns true if f1 is chronologically later than f2. Both fences must be
 * from the same context, since a seqno is not re-used across contexts.
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/ttm/ttm_bo_util.c' l='766' u='c' c='ttm_bo_pipeline_move'/>
