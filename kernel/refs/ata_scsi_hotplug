<dec f='linux-4.14.y/drivers/ata/libata.h' l='141' type='void ata_scsi_hotplug(struct work_struct * work)'/>
<use f='linux-4.14.y/drivers/ata/libata-core.c' l='6007' u='r' c='ata_port_alloc'/>
<def f='linux-4.14.y/drivers/ata/libata-scsi.c' l='4780' ll='4829' type='void ata_scsi_hotplug(struct work_struct * work)'/>
<doc f='linux-4.14.y/drivers/ata/libata-scsi.c' l='4768'>/**
 *	ata_scsi_hotplug - SCSI part of hotplug
 *	@work: Pointer to ATA port to perform SCSI hotplug on
 *
 *	Perform SCSI part of hotplug.  It&apos;s executed from a separate
 *	workqueue after EH completes.  This is necessary because SCSI
 *	hot plugging requires working EH and hot unplugging is
 *	synchronized with hot plugging with a mutex.
 *
 *	LOCKING:
 *	Kernel thread context (may sleep).
 */</doc>
<dec f='linux-4.14.y/drivers/ata/libata.h' l='141' type='void ata_scsi_hotplug(struct work_struct * work)'/>
<use f='linux-4.14.y/drivers/ata/libata-core.c' l='6007' u='r' c='ata_port_alloc'/>
<def f='linux-4.14.y/drivers/ata/libata-scsi.c' l='4780' ll='4829' type='void ata_scsi_hotplug(struct work_struct * work)'/>
<doc f='linux-4.14.y/drivers/ata/libata-scsi.c' l='4768'>/**
 *	ata_scsi_hotplug - SCSI part of hotplug
 *	@work: Pointer to ATA port to perform SCSI hotplug on
 *
 *	Perform SCSI part of hotplug.  It&apos;s executed from a separate
 *	workqueue after EH completes.  This is necessary because SCSI
 *	hot plugging requires working EH and hot unplugging is
 *	synchronized with hot plugging with a mutex.
 *
 *	LOCKING:
 *	Kernel thread context (may sleep).
 */</doc>
