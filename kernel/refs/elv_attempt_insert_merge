<dec f='linux-4.18.y/include/linux/elevator.h' l='197' type='bool elv_attempt_insert_merge(struct request_queue * , struct request * )'/>
<use f='linux-4.18.y/block/blk-mq-sched.c' l='356' u='c' c='blk_mq_sched_try_insert_merge'/>
<def f='linux-4.18.y/block/elevator.c' l='474' ll='506' type='bool elv_attempt_insert_merge(struct request_queue * q, struct request * rq)'/>
<use f='linux-4.18.y/block/elevator.c' l='668' u='c' c='__elv_add_request'/>
<doc f='linux-4.18.y/block/elevator.c' l='467'>/*
 * Attempt to do an insertion back merge. Only check for the case where
 * we can append &apos;rq&apos; to an existing request, so we can throw &apos;rq&apos; away
 * afterwards.
 *
 * Returns true if we merged, false otherwise
 */</doc>
