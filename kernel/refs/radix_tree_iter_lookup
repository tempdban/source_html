<def f='linux-4.14.y/include/linux/radix-tree.h' l='435' ll='441' type='void ** radix_tree_iter_lookup(const struct radix_tree_root * root, struct radix_tree_iter * iter, unsigned long index)'/>
<doc f='linux-4.14.y/include/linux/radix-tree.h' l='425'>/**
 * radix_tree_iter_lookup - look up an index in the radix tree
 * @root: radix tree root
 * @iter: iterator state
 * @index: key to look up
 *
 * If @index is present in the radix tree, this function returns the slot
 * containing it and updates @iter to describe the entry.  If @index is not
 * present, it returns NULL.
 */</doc>
<use f='linux-4.14.y/lib/idr.c' l='363' u='c' c='ida_remove'/>
