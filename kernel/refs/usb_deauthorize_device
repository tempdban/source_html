<dec f='linux-4.14.y/drivers/usb/core/usb.h' l='33' type='int usb_deauthorize_device(struct usb_device * )'/>
<def f='linux-4.14.y/drivers/usb/core/hub.c' l='2519' ll='2531' type='int usb_deauthorize_device(struct usb_device * usb_dev)'/>
<doc f='linux-4.14.y/drivers/usb/core/hub.c' l='2507'>/**
 * usb_deauthorize_device - deauthorize a device (usbcore-internal)
 * @usb_dev: USB device
 *
 * Move the USB device to a very basic state where interfaces are disabled
 * and the device is in fact unconfigured and unusable.
 *
 * We share a lock (that we have) with device_del(), so we need to
 * defer its call.
 *
 * Return: 0.
 */</doc>
<use f='linux-4.14.y/drivers/usb/core/sysfs.c' l='744' u='c' c='authorized_store'/>
