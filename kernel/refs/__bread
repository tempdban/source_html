<def f='linux-4.18.y/include/linux/buffer_head.h' l='393' ll='397' type='struct buffer_head * __bread(struct block_device * bdev, sector_t block, unsigned int size)'/>
<doc f='linux-4.18.y/include/linux/buffer_head.h' l='383'>/**
 *  __bread() - reads a specified block and returns the bh
 *  @bdev: the block_device to read from
 *  @block: number of block
 *  @size: size (in bytes) to read
 *
 *  Reads a specified block, and returns buffer head that contains it.
 *  The page cache is allocated from movable area so that it can be migrated.
 *  It returns NULL if the block was unreadable.
 */</doc>
<use f='linux-4.18.y/fs/ext4/super.c' l='4635' u='c' c='ext4_get_dev_journal'/>
