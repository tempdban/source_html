<dec f='linux-4.18.y/include/linux/skbuff.h' l='1875' type='struct sk_buff * skb_dequeue_tail(struct sk_buff_head * list)'/>
<doc f='linux-4.18.y/include/linux/skbuff.h' l='1867'>/**
 *	__skb_dequeue_tail - remove from the tail of the queue
 *	@list: list to dequeue from
 *
 *	Remove the tail of the list. This function does not take any locks
 *	so must be used with appropriate locks held only. The tail item is
 *	returned or %NULL if the list is empty.
 */</doc>
<def f='linux-4.18.y/net/core/skbuff.c' l='2827' ll='2836' type='struct sk_buff * skb_dequeue_tail(struct sk_buff_head * list)'/>
<dec f='linux-4.18.y/net/core/skbuff.c' l='2837' type='struct sk_buff * skb_dequeue_tail(struct sk_buff_head * )'/>
<use f='linux-4.18.y/net/core/skbuff.c' l='2837' c='skb_dequeue_tail'/>
<use f='linux-4.18.y/net/core/skbuff.c' l='2837' u='a'/>
<use f='linux-4.18.y/net/core/skbuff.c' l='2837' u='a'/>
<doc f='linux-4.18.y/net/core/skbuff.c' l='2819'>/**
 *	skb_dequeue_tail - remove from the tail of the queue
 *	@list: list to dequeue from
 *
 *	Remove the tail of the list. The list lock is taken so the function
 *	may be used safely with other locking list functions. The tail item is
 *	returned or %NULL if the list is empty.
 */</doc>
