<dec f='linux-4.14.y/include/linux/usb/composite.h' l='337' type='void usb_remove_config(struct usb_composite_dev * , struct usb_configuration * )'/>
<def f='linux-4.14.y/drivers/usb/gadget/composite.c' l='990' ll='1003' type='void usb_remove_config(struct usb_composite_dev * cdev, struct usb_configuration * config)'/>
<doc f='linux-4.14.y/drivers/usb/gadget/composite.c' l='981'>/**
 * usb_remove_config() - remove a configuration from a device.
 * @cdev: wraps the USB gadget
 * @config: the configuration
 *
 * Drivers must call usb_gadget_disconnect before calling this function
 * to disconnect the device from the host and make sure the host will not
 * try to enumerate the device while we are changing the config list.
 */</doc>
