<dec f='linux-4.14.y/drivers/gpu/drm/drm_legacy.h' l='48' type='void drm_legacy_ctxbitmap_flush(struct drm_device * dev, struct drm_file * file)'/>
<def f='linux-4.14.y/drivers/gpu/drm/drm_context.c' l='129' ll='152' type='void drm_legacy_ctxbitmap_flush(struct drm_device * dev, struct drm_file * file)'/>
<doc f='linux-4.14.y/drivers/gpu/drm/drm_context.c' l='120'>/**
 * drm_ctxbitmap_flush() - Flush all contexts owned by a file
 * @dev: DRM device to operate on
 * @file: Open file to flush contexts for
 *
 * This iterates over all contexts on @dev and drops them if they&apos;re owned by
 * @file. Note that after this call returns, new contexts might be added if
 * the file is still alive.
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_file.c' l='418' u='c' c='drm_release'/>
<dec f='linux-4.14.y/drivers/gpu/drm/drm_legacy.h' l='48' type='void drm_legacy_ctxbitmap_flush(struct drm_device * dev, struct drm_file * file)'/>
<def f='linux-4.14.y/drivers/gpu/drm/drm_context.c' l='129' ll='152' type='void drm_legacy_ctxbitmap_flush(struct drm_device * dev, struct drm_file * file)'/>
<doc f='linux-4.14.y/drivers/gpu/drm/drm_context.c' l='120'>/**
 * drm_ctxbitmap_flush() - Flush all contexts owned by a file
 * @dev: DRM device to operate on
 * @file: Open file to flush contexts for
 *
 * This iterates over all contexts on @dev and drops them if they&apos;re owned by
 * @file. Note that after this call returns, new contexts might be added if
 * the file is still alive.
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_file.c' l='418' u='c' c='drm_release'/>
