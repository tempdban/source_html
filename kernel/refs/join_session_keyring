<dec f='linux-4.18.y/security/keys/internal.h' l='167' type='long join_session_keyring(const char * name)'/>
<use f='linux-4.18.y/security/keys/keyctl.c' l='299' u='c' c='keyctl_join_session_keyring'/>
<use f='linux-4.18.y/security/keys/process_keys.c' l='595' u='c' c='lookup_user_key'/>
<use f='linux-4.18.y/security/keys/process_keys.c' l='606' u='c' c='lookup_user_key'/>
<def f='linux-4.18.y/security/keys/process_keys.c' l='770' ll='838' type='long join_session_keyring(const char * name)'/>
<doc f='linux-4.18.y/security/keys/process_keys.c' l='759'>/*
 * Join the named keyring as the session keyring if possible else attempt to
 * create a new one of that name and join that.
 *
 * If the name is NULL, an empty anonymous keyring will be installed as the
 * session keyring.
 *
 * Named session keyrings are joined with a semaphore held to prevent the
 * keyrings from going away whilst the attempt is made to going them and also
 * to prevent a race in creating compatible session keyrings.
 */</doc>
