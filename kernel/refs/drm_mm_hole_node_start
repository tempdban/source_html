<def f='linux-4.14.y/include/drm/drm_mm.h' l='278' ll='282' type='u64 drm_mm_hole_node_start(const struct drm_mm_node * hole_node)'/>
<doc f='linux-4.14.y/include/drm/drm_mm.h' l='267'>/**
 * drm_mm_hole_node_start - computes the start of the hole following @node
 * @hole_node: drm_mm_node which implicitly tracks the following hole
 *
 * This is useful for driver-specific debug dumpers. Otherwise drivers should
 * not inspect holes themselves. Drivers must check first whether a hole indeed
 * follows by looking at drm_mm_hole_follows()
 *
 * Returns:
 * Start of the subsequent hole.
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_mm.c' l='921' u='c' c='drm_mm_dump_hole'/>
<def f='linux-4.14.y/include/drm/drm_mm.h' l='278' ll='282' type='u64 drm_mm_hole_node_start(const struct drm_mm_node * hole_node)'/>
<doc f='linux-4.14.y/include/drm/drm_mm.h' l='267'>/**
 * drm_mm_hole_node_start - computes the start of the hole following @node
 * @hole_node: drm_mm_node which implicitly tracks the following hole
 *
 * This is useful for driver-specific debug dumpers. Otherwise drivers should
 * not inspect holes themselves. Drivers must check first whether a hole indeed
 * follows by looking at drm_mm_hole_follows()
 *
 * Returns:
 * Start of the subsequent hole.
 */</doc>
<use f='linux-4.14.y/drivers/gpu/drm/drm_mm.c' l='921' u='c' c='drm_mm_dump_hole'/>
