<dec f='linux-4.18.y/include/linux/attribute_container.h' l='58' type='void attribute_container_trigger(struct device * dev, int (*)(struct attribute_container *, struct device *) fn)'/>
<def f='linux-4.18.y/drivers/base/attribute_container.c' l='290' ll='303' type='void attribute_container_trigger(struct device * dev, int (*)(struct attribute_container *, struct device *) fn)'/>
<doc f='linux-4.18.y/drivers/base/attribute_container.c' l='278'>/**
 * attribute_container_trigger - trigger a function for each matching container
 *
 * @dev:  The generic device to activate the trigger for
 * @fn:	  the function to trigger
 *
 * This routine triggers a function that only needs to know the
 * matching containers (not the classdev) associated with a device.
 * It is more lightweight than attribute_container_device_trigger, so
 * should be used in preference unless the triggering function
 * actually needs to know the classdev.
 */</doc>
