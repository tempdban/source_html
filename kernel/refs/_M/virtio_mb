<dec f='linux-4.18.y/drivers/crypto/virtio/virtqueue.h' l='29'/>
<doc f='linux-4.18.y/drivers/crypto/virtio/virtqueue.h' l='22'>/*
 * Per virtio_config.h in Linux.
 *     For virtio_pci on SMP, we don&apos;t need to order with respect to MMIO
 *     accesses through relaxed memory I/O windows, so smp_mb() et al are
 *     sufficient.
 *
 */</doc>
<dec f='linux-4.18.y/drivers/net/virtio/virtqueue.h' l='28'/>
<doc f='linux-4.18.y/drivers/net/virtio/virtqueue.h' l='21'>/*
 * Per virtio_config.h in Linux.
 *     For virtio_pci on SMP, we don&apos;t need to order with respect to MMIO
 *     accesses through relaxed memory I/O windows, so smp_mb() et al are
 *     sufficient.
 *
 */</doc>
