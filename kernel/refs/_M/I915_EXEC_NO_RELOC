<dec f='linux-4.14.y/include/uapi/drm/i915_drm.h' l='900'/>
<doc f='linux-4.14.y/include/uapi/drm/i915_drm.h' l='895'>/** Provide a hint to the kernel that the command stream and auxiliary
 * state buffers already holds the correct presumed addresses and so the
 * relocation process may be skipped if no buffers need to be moved in
 * preparation for the execbuffer.
 */</doc>
<dec f='linux-4.14.y/include/uapi/drm/i915_drm.h' l='900'/>
<doc f='linux-4.14.y/include/uapi/drm/i915_drm.h' l='895'>/** Provide a hint to the kernel that the command stream and auxiliary
 * state buffers already holds the correct presumed addresses and so the
 * relocation process may be skipped if no buffers need to be moved in
 * preparation for the execbuffer.
 */</doc>
