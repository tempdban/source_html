<dec f='linux-4.18.y/lib/xz/xz_lzma2.h' l='168'/>
<use f='linux-4.18.y/lib/xz/xz_dec_lzma2.c' l='663' u='c'/>
<doc f='linux-4.18.y/lib/xz/xz_lzma2.h' l='161'>/* Match distances up to 127 are fully encoded using probabilities. Since
 * the highest two bits (distance slot) are always encoded using six bits,
 * the distances 0-3 don&apos;t need any additional bits to encode, since the
 * distance slot itself is the same as the actual distance. DIST_MODEL_START
 * indicates the first distance slot where at least one additional bit is
 * needed.
 */</doc>
