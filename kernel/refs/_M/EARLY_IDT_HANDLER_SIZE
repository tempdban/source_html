<dec f='linux-4.14.y/arch/x86/include/asm/segment.h' l='237'/>
<use f='linux-4.14.y/arch/x86/include/asm/segment.h' l='249' u='c'/>
<doc f='linux-4.14.y/arch/x86/include/asm/segment.h' l='230'>/*
 * early_idt_handler_array is an array of entry points referenced in the
 * early IDT.  For simplicity, it&apos;s a real array with one entry point
 * every nine bytes.  That leaves room for an optional &apos;push $0&apos; if the
 * vector has no error code (two bytes), a &apos;push $vector_number&apos; (two
 * bytes), and a jump to the common entry code (up to five bytes).
 */</doc>
<use f='linux-4.14.y/arch/x86/kernel/head_64.S' l='287' u='c'/>
<dec f='linux-4.14.y/arch/x86/include/asm/segment.h' l='237'/>
<use f='linux-4.14.y/arch/x86/include/asm/segment.h' l='249' u='c'/>
<doc f='linux-4.14.y/arch/x86/include/asm/segment.h' l='230'>/*
 * early_idt_handler_array is an array of entry points referenced in the
 * early IDT.  For simplicity, it&apos;s a real array with one entry point
 * every nine bytes.  That leaves room for an optional &apos;push $0&apos; if the
 * vector has no error code (two bytes), a &apos;push $vector_number&apos; (two
 * bytes), and a jump to the common entry code (up to five bytes).
 */</doc>
<use f='linux-4.14.y/arch/x86/kernel/head_64.S' l='287' u='c'/>
