<dec f='linux-4.18.y/drivers/usb/storage/usb.h' l='74'/>
<doc f='linux-4.18.y/drivers/usb/storage/usb.h' l='67'>/*
 * We provide a DMA-mapped I/O buffer for use with small USB transfers.
 * It turns out that CB[I] needs a 12-byte buffer and Bulk-only needs a
 * 31-byte buffer.  But Freecom needs a 64-byte buffer, so that&apos;s the
 * size we&apos;ll allocate.
 */</doc>
<use f='linux-4.18.y/drivers/usb/storage/usb.c' l='462' u='c'/>
<use f='linux-4.18.y/drivers/usb/storage/usb.c' l='830' u='c'/>
