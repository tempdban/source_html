<dec f='linux-4.14.y/drivers/scsi/st_options.h' l='29'/>
<use f='linux-4.14.y/drivers/scsi/st.c' l='4326' u='c'/>
<doc f='linux-4.14.y/drivers/scsi/st_options.h' l='24'>/* If ST_IN_FILE_POS is nonzero, the driver positions the tape after the
   record been read by the user program even if the tape has moved further
   because of buffered reads. Should be set to zero to support also drives
   that can&apos;t space backwards over records. NOTE: The tape will be
   spaced backwards over an &quot;accidentally&quot; crossed filemark in any case. */</doc>
<dec f='linux-4.14.y/drivers/scsi/st_options.h' l='29'/>
<use f='linux-4.14.y/drivers/scsi/st.c' l='4326' u='c'/>
<doc f='linux-4.14.y/drivers/scsi/st_options.h' l='24'>/* If ST_IN_FILE_POS is nonzero, the driver positions the tape after the
   record been read by the user program even if the tape has moved further
   because of buffered reads. Should be set to zero to support also drives
   that can&apos;t space backwards over records. NOTE: The tape will be
   spaced backwards over an &quot;accidentally&quot; crossed filemark in any case. */</doc>
