<dec f='linux-4.14.y/arch/x86/include/uapi/asm/mman.h' l='16'/>
<use f='linux-4.14.y/include/linux/mman.h' l='42' u='a'/>
<doc f='linux-4.14.y/arch/x86/include/uapi/asm/mman.h' l='8'>/*
 * Take the 4 protection key bits out of the vma-&gt;vm_flags
 * value and turn them in to the bits that we can put in
 * to a pte.
 *
 * Only override these if Protection Keys are available
 * (which is only on 64-bit).
 */</doc>
<use f='linux-4.14.y/mm/mmap.c' l='107' u='c'/>
<dec f='linux-4.14.y/arch/x86/include/uapi/asm/mman.h' l='16'/>
<use f='linux-4.14.y/include/linux/mman.h' l='42' u='a'/>
<doc f='linux-4.14.y/arch/x86/include/uapi/asm/mman.h' l='8'>/*
 * Take the 4 protection key bits out of the vma-&gt;vm_flags
 * value and turn them in to the bits that we can put in
 * to a pte.
 *
 * Only override these if Protection Keys are available
 * (which is only on 64-bit).
 */</doc>
<use f='linux-4.14.y/mm/mmap.c' l='107' u='c'/>
