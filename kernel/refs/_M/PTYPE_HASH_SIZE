<dec f='linux-4.14.y/include/linux/netdevice.h' l='4455'/>
<doc f='linux-4.14.y/include/linux/netdevice.h' l='4428'>/*
 *	The list of packet types we will receive (as opposed to discard)
 *	and the routines to invoke.
 *
 *	Why 16. Because with 16 the only overlap we get on a hash of the
 *	low nibble of the protocol value is RARP/SNAP/X.25.
 *
 *      NOTE:  That is no longer true with the addition of VLAN tags.  Not
 *             sure which should go first, but I bet it won&apos;t make much
 *             difference if we are running VLANs.  The good news is that
 *             this protocol won&apos;t be in the list unless compiled in, so
 *             the average user (w/out VLANs) will not be adversely affected.
 *             --BLG
 *
 *		0800	IP
 *		8100    802.1Q VLAN
 *		0001	802.3
 *		0002	AX.25
 *		0004	802.2
 *		8035	RARP
 *		0005	SNAP
 *		0805	X.25
 *		0806	ARP
 *		8137	IPX
 *		0009	Localtalk
 *		86DD	IPv6
 */</doc>
<use f='linux-4.14.y/net/core/dev.c' l='159' u='c'/>
<use f='linux-4.14.y/net/core/dev.c' l='8723' u='c'/>
<use f='linux-4.14.y/net/core/net-procfs.c' l='14' u='c'/>
<use f='linux-4.14.y/net/core/net-procfs.c' l='224' u='c'/>
<use f='linux-4.14.y/net/core/net-procfs.c' l='262' u='c'/>
<dec f='linux-4.14.y/include/linux/netdevice.h' l='4455'/>
<doc f='linux-4.14.y/include/linux/netdevice.h' l='4428'>/*
 *	The list of packet types we will receive (as opposed to discard)
 *	and the routines to invoke.
 *
 *	Why 16. Because with 16 the only overlap we get on a hash of the
 *	low nibble of the protocol value is RARP/SNAP/X.25.
 *
 *      NOTE:  That is no longer true with the addition of VLAN tags.  Not
 *             sure which should go first, but I bet it won&apos;t make much
 *             difference if we are running VLANs.  The good news is that
 *             this protocol won&apos;t be in the list unless compiled in, so
 *             the average user (w/out VLANs) will not be adversely affected.
 *             --BLG
 *
 *		0800	IP
 *		8100    802.1Q VLAN
 *		0001	802.3
 *		0002	AX.25
 *		0004	802.2
 *		8035	RARP
 *		0005	SNAP
 *		0805	X.25
 *		0806	ARP
 *		8137	IPX
 *		0009	Localtalk
 *		86DD	IPv6
 */</doc>
<use f='linux-4.14.y/net/core/dev.c' l='159' u='c'/>
<use f='linux-4.14.y/net/core/dev.c' l='8723' u='c'/>
<use f='linux-4.14.y/net/core/net-procfs.c' l='14' u='c'/>
<use f='linux-4.14.y/net/core/net-procfs.c' l='224' u='c'/>
<use f='linux-4.14.y/net/core/net-procfs.c' l='262' u='c'/>
