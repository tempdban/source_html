<dec f='linux-4.14.y/net/openvswitch/vport.h' l='53' type='u32 ovs_vport_find_upcall_portid(const struct vport * , struct sk_buff * )'/>
<use f='linux-4.14.y/net/openvswitch/datapath.c' l='276' u='c' c='ovs_dp_process_packet'/>
<def f='linux-4.14.y/net/openvswitch/vport.c' l='413' ll='427' type='u32 ovs_vport_find_upcall_portid(const struct vport * vport, struct sk_buff * skb)'/>
<doc f='linux-4.14.y/net/openvswitch/vport.c' l='402'>/**
 *	ovs_vport_find_upcall_portid - find the upcall portid to send upcall.
 *
 * @vport: vport from which the missed packet is received.
 * @skb: skb that the missed packet was received.
 *
 * Uses the skb_get_hash() to select the upcall portid to send the
 * upcall.
 *
 * Returns the portid of the target socket.  Must be called with rcu_read_lock.
 */</doc>
