<def f='linux-4.14.y/kernel/sched/sched.h' l='1039' ll='1050' type='struct sched_domain * highest_flag_domain(int cpu, int flag)'/>
<doc f='linux-4.14.y/kernel/sched/sched.h' l='1030'>/**
 * highest_flag_domain - Return highest sched_domain containing flag.
 * @cpu:	The cpu whose highest level of sched domain is to
 *		be returned.
 * @flag:	The flag to check for the highest sched_domain
 *		for the given cpu.
 *
 * Returns the highest sched_domain of a cpu which contains the given flag.
 */</doc>
<use f='linux-4.14.y/kernel/sched/topology.c' l='416' u='c' c='update_top_cache_domain'/>
<use f='linux-4.14.y/kernel/sched/topology.c' l='431' u='c' c='update_top_cache_domain'/>
<def f='linux-4.14.y/kernel/sched/sched.h' l='1039' ll='1050' type='struct sched_domain * highest_flag_domain(int cpu, int flag)'/>
<doc f='linux-4.14.y/kernel/sched/sched.h' l='1030'>/**
 * highest_flag_domain - Return highest sched_domain containing flag.
 * @cpu:	The cpu whose highest level of sched domain is to
 *		be returned.
 * @flag:	The flag to check for the highest sched_domain
 *		for the given cpu.
 *
 * Returns the highest sched_domain of a cpu which contains the given flag.
 */</doc>
<use f='linux-4.14.y/kernel/sched/topology.c' l='416' u='c' c='update_top_cache_domain'/>
<use f='linux-4.14.y/kernel/sched/topology.c' l='431' u='c' c='update_top_cache_domain'/>
