<dec f='linux-4.14.y/include/linux/cpufreq.h' l='197' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * policy)'/>
<def f='linux-4.14.y/drivers/cpufreq/cpufreq.c' l='459' ll='476' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * policy)'/>
<dec f='linux-4.14.y/drivers/cpufreq/cpufreq.c' l='477' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * )'/>
<use f='linux-4.14.y/drivers/cpufreq/cpufreq.c' l='477' c='cpufreq_enable_fast_switch'/>
<use f='linux-4.14.y/drivers/cpufreq/cpufreq.c' l='477' u='a'/>
<use f='linux-4.14.y/drivers/cpufreq/cpufreq.c' l='477' u='a'/>
<doc f='linux-4.14.y/drivers/cpufreq/cpufreq.c' l='448'>/**
 * cpufreq_enable_fast_switch - Enable fast frequency switching for policy.
 * @policy: cpufreq policy to enable fast frequency switching for.
 *
 * Try to enable fast frequency switching for @policy.
 *
 * The attempt will fail if there is at least one transition notifier registered
 * at this point, as fast frequency switching is quite fundamentally at odds
 * with transition notifiers.  Thus if successful, it will make registration of
 * transition notifiers fail going forward.
 */</doc>
