<dec f='linux-4.18.y/include/linux/cpufreq.h' l='197' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * policy)'/>
<def f='linux-4.18.y/drivers/cpufreq/cpufreq.c' l='450' ll='467' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * policy)'/>
<dec f='linux-4.18.y/drivers/cpufreq/cpufreq.c' l='468' type='void cpufreq_enable_fast_switch(struct cpufreq_policy * )'/>
<use f='linux-4.18.y/drivers/cpufreq/cpufreq.c' l='468' c='cpufreq_enable_fast_switch'/>
<use f='linux-4.18.y/drivers/cpufreq/cpufreq.c' l='468' u='a'/>
<use f='linux-4.18.y/drivers/cpufreq/cpufreq.c' l='468' u='a'/>
<doc f='linux-4.18.y/drivers/cpufreq/cpufreq.c' l='439'>/**
 * cpufreq_enable_fast_switch - Enable fast frequency switching for policy.
 * @policy: cpufreq policy to enable fast frequency switching for.
 *
 * Try to enable fast frequency switching for @policy.
 *
 * The attempt will fail if there is at least one transition notifier registered
 * at this point, as fast frequency switching is quite fundamentally at odds
 * with transition notifiers.  Thus if successful, it will make registration of
 * transition notifiers fail going forward.
 */</doc>
