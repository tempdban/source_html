<dec f='linux-4.14.y/include/linux/clk.h' l='485' type='bool clk_has_parent(struct clk * clk, struct clk * parent)'/>
<doc f='linux-4.14.y/include/linux/clk.h' l='475'>/**
 * clk_has_parent - check if a clock is a possible parent for another
 * @clk: clock source
 * @parent: parent clock source
 *
 * This function can be used in drivers that need to check that a clock can be
 * the parent of another without actually changing the parent.
 *
 * Returns true if @parent is a possible parent for @clk, false otherwise.
 */</doc>
<def f='linux-4.14.y/drivers/clk/clk.c' l='1767' ll='1788' type='bool clk_has_parent(struct clk * clk, struct clk * parent)'/>
<dec f='linux-4.14.y/drivers/clk/clk.c' l='1789' type='bool clk_has_parent(struct clk * , struct clk * )'/>
<use f='linux-4.14.y/drivers/clk/clk.c' l='1789' c='clk_has_parent'/>
<use f='linux-4.14.y/drivers/clk/clk.c' l='1789' u='a'/>
<use f='linux-4.14.y/drivers/clk/clk.c' l='1789' u='a'/>
<doc f='linux-4.14.y/drivers/clk/clk.c' l='1757'>/**
 * clk_has_parent - check if a clock is a possible parent for another
 * @clk: clock source
 * @parent: parent clock source
 *
 * This function can be used in drivers that need to check that a clock can be
 * the parent of another without actually changing the parent.
 *
 * Returns true if @parent is a possible parent for @clk, false otherwise.
 */</doc>
<dec f='linux-4.14.y/include/linux/clk.h' l='485' type='bool clk_has_parent(struct clk * clk, struct clk * parent)'/>
<doc f='linux-4.14.y/include/linux/clk.h' l='475'>/**
 * clk_has_parent - check if a clock is a possible parent for another
 * @clk: clock source
 * @parent: parent clock source
 *
 * This function can be used in drivers that need to check that a clock can be
 * the parent of another without actually changing the parent.
 *
 * Returns true if @parent is a possible parent for @clk, false otherwise.
 */</doc>
<def f='linux-4.14.y/drivers/clk/clk.c' l='1767' ll='1788' type='bool clk_has_parent(struct clk * clk, struct clk * parent)'/>
<dec f='linux-4.14.y/drivers/clk/clk.c' l='1789' type='bool clk_has_parent(struct clk * , struct clk * )'/>
<use f='linux-4.14.y/drivers/clk/clk.c' l='1789' c='clk_has_parent'/>
<use f='linux-4.14.y/drivers/clk/clk.c' l='1789' u='a'/>
<use f='linux-4.14.y/drivers/clk/clk.c' l='1789' u='a'/>
<doc f='linux-4.14.y/drivers/clk/clk.c' l='1757'>/**
 * clk_has_parent - check if a clock is a possible parent for another
 * @clk: clock source
 * @parent: parent clock source
 *
 * This function can be used in drivers that need to check that a clock can be
 * the parent of another without actually changing the parent.
 *
 * Returns true if @parent is a possible parent for @clk, false otherwise.
 */</doc>
