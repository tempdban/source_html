<dec f='linux-4.18.y/include/linux/page-isolation.h' l='49' type='int start_isolate_page_range(unsigned long start_pfn, unsigned long end_pfn, unsigned int migratetype, bool skip_hwpoisoned_pages)'/>
<doc f='linux-4.18.y/include/linux/page-isolation.h' l='39'>/*
 * Changes migrate type in [start_pfn, end_pfn) to be MIGRATE_ISOLATE.
 * If specified range includes migrate types other than MOVABLE or CMA,
 * this will fail with -EBUSY.
 *
 * For isolating all pages in the range finally, the caller have to
 * free all pages in the range. test_page_isolated() can be used for
 * test it.
 */</doc>
