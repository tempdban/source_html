<def f='linux-4.14.y/include/linux/fs.h' l='2621' ll='2626' type='void filemap_set_wb_err(struct address_space * mapping, int err)'/>
<doc f='linux-4.14.y/include/linux/fs.h' l='2607'>/**
 * filemap_set_wb_err - set a writeback error on an address_space
 * @mapping: mapping in which to set writeback error
 * @err: error to be set in mapping
 *
 * When writeback fails in some way, we must record that error so that
 * userspace can be informed when fsync and the like are called.  We endeavor
 * to report errors on any file that was open at the time of the error.  Some
 * internal callers also need to know when writeback errors have occurred.
 *
 * When a writeback error occurs, most filesystems will want to call
 * filemap_set_wb_err to record the error in the mapping so that it will be
 * automatically reported whenever fsync is called on the file.
 */</doc>
<use f='linux-4.14.y/include/linux/pagemap.h' l='52' u='c' c='mapping_set_error'/>
<def f='linux-4.14.y/include/linux/fs.h' l='2621' ll='2626' type='void filemap_set_wb_err(struct address_space * mapping, int err)'/>
<use f='linux-4.14.y/include/linux/pagemap.h' l='52' u='c' c='mapping_set_error'/>
<doc f='linux-4.14.y/include/linux/fs.h' l='2607'>/**
 * filemap_set_wb_err - set a writeback error on an address_space
 * @mapping: mapping in which to set writeback error
 * @err: error to be set in mapping
 *
 * When writeback fails in some way, we must record that error so that
 * userspace can be informed when fsync and the like are called.  We endeavor
 * to report errors on any file that was open at the time of the error.  Some
 * internal callers also need to know when writeback errors have occurred.
 *
 * When a writeback error occurs, most filesystems will want to call
 * filemap_set_wb_err to record the error in the mapping so that it will be
 * automatically reported whenever fsync is called on the file.
 */</doc>
