<def f='linux-4.14.y/arch/x86/include/asm/timer.h' l='29' ll='33'/>
<size>16</size>
<doc f='linux-4.14.y/arch/x86/include/asm/timer.h' l='18'>/*
 * We use the full linear equation: f(x) = a + b*x, in order to allow
 * a continuous function in the face of dynamic freq changes.
 *
 * Continuity means that when our frequency changes our slope (b); we want to
 * ensure that: f(t) == f&apos;(t), which gives: a + b*t == a&apos; + b&apos;*t.
 *
 * Without an offset (a) the above would not be possible.
 *
 * See the comment near cycles_2_ns() for details on how we compute (b).
 */</doc>
<mbr r='cyc2ns_data::cyc2ns_mul' o='0' t='u32'/>
<mbr r='cyc2ns_data::cyc2ns_shift' o='32' t='u32'/>
<mbr r='cyc2ns_data::cyc2ns_offset' o='64' t='u64'/>
<def f='linux-4.14.y/arch/x86/include/asm/timer.h' l='29' ll='33'/>
<size>16</size>
<doc f='linux-4.14.y/arch/x86/include/asm/timer.h' l='18'>/*
 * We use the full linear equation: f(x) = a + b*x, in order to allow
 * a continuous function in the face of dynamic freq changes.
 *
 * Continuity means that when our frequency changes our slope (b); we want to
 * ensure that: f(t) == f&apos;(t), which gives: a + b*t == a&apos; + b&apos;*t.
 *
 * Without an offset (a) the above would not be possible.
 *
 * See the comment near cycles_2_ns() for details on how we compute (b).
 */</doc>
<mbr r='cyc2ns_data::cyc2ns_mul' o='0' t='u32'/>
<mbr r='cyc2ns_data::cyc2ns_shift' o='32' t='u32'/>
<mbr r='cyc2ns_data::cyc2ns_offset' o='64' t='u64'/>
