<dec f='linux-4.14.y/include/linux/swapfile.h' l='12' type='int try_to_unuse(unsigned int , bool , unsigned long )'/>
<def f='linux-4.14.y/mm/swapfile.c' l='2024' ll='2246' type='int try_to_unuse(unsigned int type, bool frontswap, unsigned long pages_to_unuse)'/>
<use f='linux-4.14.y/mm/swapfile.c' l='2601' u='c' c='SYSC_swapoff'/>
<doc f='linux-4.14.y/mm/swapfile.c' l='2016'>/*
 * We completely avoid races by reading each swap page in advance,
 * and then search for the process using it.  All the necessary
 * page table adjustments can then be made atomically.
 *
 * if the boolean frontswap is true, only unuse pages_to_unuse pages;
 * pages_to_unuse==0 means all pages; ignored if frontswap is false
 */</doc>
<dec f='linux-4.14.y/include/linux/swapfile.h' l='12' type='int try_to_unuse(unsigned int , bool , unsigned long )'/>
<def f='linux-4.14.y/mm/swapfile.c' l='2024' ll='2246' type='int try_to_unuse(unsigned int type, bool frontswap, unsigned long pages_to_unuse)'/>
<use f='linux-4.14.y/mm/swapfile.c' l='2601' u='c' c='SYSC_swapoff'/>
<doc f='linux-4.14.y/mm/swapfile.c' l='2016'>/*
 * We completely avoid races by reading each swap page in advance,
 * and then search for the process using it.  All the necessary
 * page table adjustments can then be made atomically.
 *
 * if the boolean frontswap is true, only unuse pages_to_unuse pages;
 * pages_to_unuse==0 means all pages; ignored if frontswap is false
 */</doc>
