<dec f='linux-4.14.y/include/linux/kmsg_dump.h' l='58' type='bool kmsg_dump_get_line_nolock(struct kmsg_dumper * dumper, bool syslog, char * line, size_t size, size_t * len)'/>
<use f='linux-4.14.y/kernel/debug/kdb/kdb_main.c' l='2130' u='c' c='kdb_dmesg'/>
<use f='linux-4.14.y/kernel/debug/kdb/kdb_main.c' l='2163' u='c' c='kdb_dmesg'/>
<def f='linux-4.14.y/kernel/printk/printk.c' l='2899' ll='2929' type='bool kmsg_dump_get_line_nolock(struct kmsg_dumper * dumper, bool syslog, char * line, size_t size, size_t * len)'/>
<use f='linux-4.14.y/kernel/printk/printk.c' l='2955' u='c' c='kmsg_dump_get_line'/>
<doc f='linux-4.14.y/kernel/printk/printk.c' l='2880'>/**
 * kmsg_dump_get_line_nolock - retrieve one kmsg log line (unlocked version)
 * @dumper: registered kmsg dumper
 * @syslog: include the &quot;&lt;4&gt;&quot; prefixes
 * @line: buffer to copy the line to
 * @size: maximum size of the buffer
 * @len: length of line placed into buffer
 *
 * Start at the beginning of the kmsg buffer, with the oldest kmsg
 * record, and copy one record into the provided buffer.
 *
 * Consecutive calls will return the next available record moving
 * towards the end of the buffer with the youngest messages.
 *
 * A return value of FALSE indicates that there are no more records to
 * read.
 *
 * The function is similar to kmsg_dump_get_line(), but grabs no locks.
 */</doc>
