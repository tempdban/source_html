<dec f='include/mbedtls/entropy.h' l='201' type='int mbedtls_entropy_func(void * data, unsigned char * output, size_t len)'/>
<use f='vpp_1804/src/plugins/tlsmbedtls/tls_mbedtls.c' l='136' u='r' c='tls_init_ctr_seed_drbgs'/>
<doc f='include/mbedtls/entropy.h' l='190'>/**
 * \brief           Retrieve entropy from the accumulator
 *                  (Maximum length: MBEDTLS_ENTROPY_BLOCK_SIZE)
 *                  (Thread-safe if MBEDTLS_THREADING_C is enabled)
 *
 * \param data      Entropy context
 * \param output    Buffer to fill
 * \param len       Number of bytes desired, must be at most MBEDTLS_ENTROPY_BLOCK_SIZE
 *
 * \return          0 if successful, or MBEDTLS_ERR_ENTROPY_SOURCE_FAILED
 */</doc>
