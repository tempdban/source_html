<dec f='include/mbedtls/config.h' l='2137'/>
<use f='include/mbedtls/check_config.h' l='495' u='a'/>
<use f='include/mbedtls/check_config.h' l='500' u='a'/>
<use f='include/mbedtls/check_config.h' l='505' u='a'/>
<doc f='include/mbedtls/config.h' l='2117'>/**
 * \def MBEDTLS_MD5_C
 *
 * Enable the MD5 hash algorithm.
 *
 * Module:  library/md5.c
 * Caller:  library/md.c
 *          library/pem.c
 *          library/ssl_tls.c
 *
 * This module is required for SSL/TLS up to version 1.1, and for TLS 1.2
 * depending on the handshake parameters. Further, it is used for checking
 * MD5-signed certificates, and for PBKDF1 when decrypting PEM-encoded
 * encrypted keys.
 *
 * \warning   MD5 is considered a weak message digest and its use constitutes a
 *            security risk. If possible, we recommend avoiding dependencies on
 *            it, and considering stronger message digests instead.
 *
 */</doc>
