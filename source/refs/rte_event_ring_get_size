<def f='dpdk_18.05/lib/librte_eventdev/rte_event_ring.h' l='259' ll='263' type='unsigned int rte_event_ring_get_size(const struct rte_event_ring * r)'/>
<doc f='dpdk_18.05/lib/librte_eventdev/rte_event_ring.h' l='249'>/**
 * Return the size of the event ring.
 *
 * @param r
 *   A pointer to the ring structure.
 * @return
 *   The size of the data store used by the ring.
 *   NOTE: this is not the same as the usable space in the ring. To query that
 *   use ``rte_ring_get_capacity()``.
 */</doc>
<def f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/lib/librte_eventdev/rte_event_ring.h' l='259' ll='263' type='unsigned int rte_event_ring_get_size(const struct rte_event_ring * r)'/>
<doc f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/lib/librte_eventdev/rte_event_ring.h' l='249'>/**
 * Return the size of the event ring.
 *
 * @param r
 *   A pointer to the ring structure.
 * @return
 *   The size of the data store used by the ring.
 *   NOTE: this is not the same as the usable space in the ring. To query that
 *   use ``rte_ring_get_capacity()``.
 */</doc>
