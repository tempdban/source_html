<dec f='dpdk/lib/librte_hash/rte_hash.h' l='143' type='int rte_hash_add_key_data(const struct rte_hash * h, const void * key, void * data)'/>
<doc f='dpdk/lib/librte_hash/rte_hash.h' l='126'>/**
 * Add a key-value pair to an existing hash table.
 * This operation is not multi-thread safe
 * and should only be called from one thread.
 *
 * @param h
 *   Hash table to add the key to.
 * @param key
 *   Key to add to the hash table.
 * @param data
 *   Data to add to the hash table.
 * @return
 *   - 0 if added successfully
 *   - -EINVAL if the parameters are invalid.
 *   - -ENOSPC if there is no space in the hash for this key.
 */</doc>
<def f='dpdk/lib/librte_hash/rte_cuckoo_hash.c' l='680' ll='692' type='int rte_hash_add_key_data(const struct rte_hash * h, const void * key, void * data)'/>
<use f='dpdk/lib/librte_vhost/vhost_crypto.c' l='371' u='c' c='vhost_crypto_create_sess'/>
