<dec f='include/mbedtls/cipher.h' l='632' type='int mbedtls_cipher_write_tag(mbedtls_cipher_context_t * ctx, unsigned char * tag, size_t tag_len)'/>
<doc f='include/mbedtls/cipher.h' l='621'>/**
 * \brief               This function writes a tag for AEAD ciphers.
 *                      Only supported with GCM.
 *                      Must be called after mbedtls_cipher_finish().
 *
 * \param ctx           The generic cipher context.
 * \param tag           The buffer to write the tag to.
 * \param tag_len       The length of the tag to write.
 *
 * \return              \c 0 on success, or a specific error code on failure.
 */</doc>
