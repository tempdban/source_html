<def f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='441' ll='447' type='unsigned int rte_ring_sp_enqueue_bulk(struct rte_ring * r, void *const * obj_table, unsigned int n, unsigned int * free_space)'/>
<use f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='510' u='c' c='rte_ring_sp_enqueue'/>
<doc f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='426'>/**
 * Enqueue several objects on a ring (NOT multi-producers safe).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects).
 * @param n
 *   The number of objects to add in the ring from the obj_table.
 * @param free_space
 *   if non-NULL, returns the amount of space in the ring after the
 *   enqueue operation has finished.
 * @return
 *   The number of objects enqueued, either 0 or n
 */</doc>
<use f='dpdk_18.05/drivers/mempool/ring/rte_mempool_ring.c' l='24' u='c' c='common_ring_sp_enqueue'/>
