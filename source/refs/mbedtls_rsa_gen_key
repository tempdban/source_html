<dec f='include/mbedtls/rsa.h' l='412' type='int mbedtls_rsa_gen_key(mbedtls_rsa_context * ctx, int (*)(void *, unsigned char *, size_t) f_rng, void * p_rng, unsigned int nbits, int exponent)'/>
<doc f='include/mbedtls/rsa.h' l='397'>/**
 * \brief          This function generates an RSA keypair.
 *
 * \param ctx      The RSA context used to hold the key.
 * \param f_rng    The RNG function.
 * \param p_rng    The RNG parameter.
 * \param nbits    The size of the public key in bits.
 * \param exponent The public exponent. For example, 65537.
 *
 * \note           mbedtls_rsa_init() must be called before this function,
 *                 to set up the RSA context.
 *
 * \return         \c 0 on success, or an \c MBEDTLS_ERR_RSA_XXX error code
                   on failure.
 */</doc>
