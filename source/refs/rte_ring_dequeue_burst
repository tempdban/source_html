<def f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='931' ll='938' type='unsigned int rte_ring_dequeue_burst(struct rte_ring * r, void ** obj_table, unsigned int n, unsigned int * available)'/>
<use f='dpdk_18.05/app/pdump/main.c' l='443' u='c' c='pdump_rxtx'/>
<doc f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='912'>/**
 * Dequeue multiple objects from a ring up to a maximum number.
 *
 * This function calls the multi-consumers or the single-consumer
 * version, depending on the default behaviour that was specified at
 * ring creation time (see flags).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects) that will be filled.
 * @param n
 *   The number of objects to dequeue from the ring to the obj_table.
 * @param available
 *   If non-NULL, returns the number of remaining ring entries after the
 *   dequeue has finished.
 * @return
 *   - Number of objects dequeued
 */</doc>
<use f='dpdk_18.05/drivers/baseband/null/bbdev_null.c' l='177' u='c' c='dequeue_dec_ops'/>
<use f='dpdk_18.05/drivers/baseband/null/bbdev_null.c' l='190' u='c' c='dequeue_enc_ops'/>
<use f='dpdk_18.05/drivers/crypto/null/null_crypto_pmd.c' l='149' u='c' c='null_crypto_pmd_dequeue_burst'/>
<use f='dpdk_18.05/drivers/crypto/scheduler/scheduler_multicore.c' l='94' u='c' c='schedule_dequeue'/>
<use f='dpdk_18.05/drivers/crypto/scheduler/scheduler_multicore.c' l='199' u='c' c='mc_scheduler_worker'/>
<use f='dpdk_18.05/drivers/net/ring/rte_eth_ring.c' l='77' u='c' c='eth_ring_rx'/>
<use f='dpdk_18.05/lib/librte_eventdev/rte_event_timer_adapter.c' l='717' u='c' c='sw_event_timer_adapter_service_func'/>
<def f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/lib/librte_ring/rte_ring.h' l='935' ll='942' type='unsigned int rte_ring_dequeue_burst(struct rte_ring * r, void ** obj_table, unsigned int n, unsigned int * available)'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/app/pdump/main.c' l='469' u='c' c='pdump_rxtx'/>
<doc f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/lib/librte_ring/rte_ring.h' l='916'>/**
 * Dequeue multiple objects from a ring up to a maximum number.
 *
 * This function calls the multi-consumers or the single-consumer
 * version, depending on the default behaviour that was specified at
 * ring creation time (see flags).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects) that will be filled.
 * @param n
 *   The number of objects to dequeue from the ring to the obj_table.
 * @param available
 *   If non-NULL, returns the number of remaining ring entries after the
 *   dequeue has finished.
 * @return
 *   - Number of objects dequeued
 */</doc>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/bbdev/null/bbdev_null.c' l='176' u='c' c='dequeue_dec_ops'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/bbdev/null/bbdev_null.c' l='189' u='c' c='dequeue_enc_ops'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/crypto/aesni_gcm/aesni_gcm_pmd.c' l='412' u='c' c='aesni_gcm_pmd_dequeue_burst'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/crypto/null/null_crypto_pmd.c' l='149' u='c' c='null_crypto_pmd_dequeue_burst'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/crypto/scheduler/scheduler_multicore.c' l='94' u='c' c='schedule_dequeue'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/crypto/scheduler/scheduler_multicore.c' l='199' u='c' c='mc_scheduler_worker'/>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/net/ring/rte_eth_ring.c' l='71' u='c' c='eth_ring_rx'/>
<def f='vpp_1804/build-root/install-vpp_debug-native/dpdk/include/dpdk/rte_ring.h' l='935' ll='942' type='unsigned int rte_ring_dequeue_burst(struct rte_ring * r, void ** obj_table, unsigned int n, unsigned int * available)'/>
<doc f='vpp_1804/build-root/install-vpp_debug-native/dpdk/include/dpdk/rte_ring.h' l='916'>/**
 * Dequeue multiple objects from a ring up to a maximum number.
 *
 * This function calls the multi-consumers or the single-consumer
 * version, depending on the default behaviour that was specified at
 * ring creation time (see flags).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects) that will be filled.
 * @param n
 *   The number of objects to dequeue from the ring to the obj_table.
 * @param available
 *   If non-NULL, returns the number of remaining ring entries after the
 *   dequeue has finished.
 * @return
 *   - Number of objects dequeued
 */</doc>
