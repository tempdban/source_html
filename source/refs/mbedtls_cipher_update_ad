<dec f='include/mbedtls/cipher.h' l='562' type='int mbedtls_cipher_update_ad(mbedtls_cipher_context_t * ctx, const unsigned char * ad, size_t ad_len)'/>
<doc f='include/mbedtls/cipher.h' l='551'>/**
 * \brief               This function adds additional data for AEAD ciphers.
 *                      Only supported with GCM. Must be called
 *                      exactly once, after mbedtls_cipher_reset().
 *
 * \param ctx           The generic cipher context.
 * \param ad            The additional data to use.
 * \param ad_len        the Length of \p ad.
 *
 * \return              \c 0 on success, or a specific error code on failure.
 */</doc>
