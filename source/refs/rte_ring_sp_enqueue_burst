<def f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='824' ll='830' type='unsigned int rte_ring_sp_enqueue_burst(struct rte_ring * r, void *const * obj_table, unsigned int n, unsigned int * free_space)'/>
<doc f='dpdk_18.05/lib/librte_ring/rte_ring.h' l='809'>/**
 * Enqueue several objects on a ring (NOT multi-producers safe).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects).
 * @param n
 *   The number of objects to add in the ring from the obj_table.
 * @param free_space
 *   if non-NULL, returns the amount of space in the ring after the
 *   enqueue operation has finished.
 * @return
 *   - n: Actual number of objects enqueued.
 */</doc>
<use f='dpdk_18.05/drivers/crypto/scheduler/scheduler_pmd_private.h' l='94' u='c' c='scheduler_order_insert'/>
<use f='dpdk_18.05/lib/librte_port/rte_port_ras.c' l='140' u='c' c='send_burst'/>
<use f='dpdk_18.05/lib/librte_port/rte_port_ring.c' l='216' u='c' c='send_burst'/>
<use f='dpdk_18.05/lib/librte_port/rte_port_ring.c' l='297' u='c' c='rte_port_ring_writer_tx_bulk_internal'/>
<use f='dpdk_18.05/lib/librte_port/rte_port_ring.c' l='492' u='c' c='send_burst_nodrop'/>
<use f='dpdk_18.05/lib/librte_port/rte_port_ring.c' l='502' u='c' c='send_burst_nodrop'/>
<use f='dpdk_18.05/lib/librte_port/rte_port_ring.c' l='615' u='c' c='rte_port_ring_writer_nodrop_tx_bulk_internal'/>
<def f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/lib/librte_ring/rte_ring.h' l='828' ll='834' type='unsigned int rte_ring_sp_enqueue_burst(struct rte_ring * r, void *const * obj_table, unsigned int n, unsigned int * free_space)'/>
<doc f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/lib/librte_ring/rte_ring.h' l='813'>/**
 * Enqueue several objects on a ring (NOT multi-producers safe).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects).
 * @param n
 *   The number of objects to add in the ring from the obj_table.
 * @param free_space
 *   if non-NULL, returns the amount of space in the ring after the
 *   enqueue operation has finished.
 * @return
 *   - n: Actual number of objects enqueued.
 */</doc>
<use f='vpp_1804/build-root/build-vpp_debug-native/dpdk/dpdk-stable-18.02.1/drivers/crypto/scheduler/scheduler_pmd_private.h' l='94' u='c' c='scheduler_order_insert'/>
<def f='vpp_1804/build-root/install-vpp_debug-native/dpdk/include/dpdk/rte_ring.h' l='828' ll='834' type='unsigned int rte_ring_sp_enqueue_burst(struct rte_ring * r, void *const * obj_table, unsigned int n, unsigned int * free_space)'/>
<doc f='vpp_1804/build-root/install-vpp_debug-native/dpdk/include/dpdk/rte_ring.h' l='813'>/**
 * Enqueue several objects on a ring (NOT multi-producers safe).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects).
 * @param n
 *   The number of objects to add in the ring from the obj_table.
 * @param free_space
 *   if non-NULL, returns the amount of space in the ring after the
 *   enqueue operation has finished.
 * @return
 *   - n: Actual number of objects enqueued.
 */</doc>
<use f='vpp_1804/src/plugins/dpdk/device/device.c' l='258' u='c' c='tx_burst_vector_internal'/>
