<dec f='master/drivers/net/cxgbe/base/common.h' l='356' type='int t4_fixup_host_params_compat(struct adapter * adap, unsigned int page_size, unsigned int cache_line_size, enum chip_type chip_compat)'/>
<def f='master/drivers/net/cxgbe/base/t4_hw.c' l='3715' ll='3857' type='int t4_fixup_host_params_compat(struct adapter * adap, unsigned int page_size, unsigned int cache_line_size, enum chip_type chip_compat)'/>
<use f='master/drivers/net/cxgbe/base/t4_hw.c' l='3874' u='c' c='t4_fixup_host_params'/>
<doc f='master/drivers/net/cxgbe/base/t4_hw.c' l='3699'>/**
 * t4_fixup_host_params_compat - fix up host-dependent parameters
 * @adap: the adapter
 * @page_size: the host&apos;s Base Page Size
 * @cache_line_size: the host&apos;s Cache Line Size
 * @chip_compat: maintain compatibility with designated chip
 *
 * Various registers in the chip contain values which are dependent on the
 * host&apos;s Base Page and Cache Line Sizes.  This function will fix all of
 * those registers with the appropriate values as passed in ...
 *
 * @chip_compat is used to limit the set of changes that are made
 * to be compatible with the indicated chip release.  This is used by
 * drivers to maintain compatibility with chip register settings when
 * the drivers haven&apos;t [yet] been updated with new chip support.
 */</doc>
<use f='master/drivers/net/cxgbe/cxgbe_main.c' l='827' u='c' c='adap_init0_tweaks'/>
