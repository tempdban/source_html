<dec f='master/drivers/bus/vmbus/rte_bus_vmbus.h' l='325' type='int rte_vmbus_chan_recv(struct vmbus_channel * chan, void * data, uint32_t * len, uint64_t * request_id)'/>
<doc f='master/drivers/bus/vmbus/rte_bus_vmbus.h' l='309'>/**
 * Receive response to request on the given channel
 * skips the channel header.
 *
 * @param channel
 *	Pointer to vmbus_channel structure.
 * @param data
 *	Pointer to the buffer you want to receive the data into.
 * @param len
 *	Pointer to size of receive buffer (in/out)
 * @param
 *	Pointer to received transaction_id
 * @return
 *   On success, returns 0
 *   On failure, returns negative errno.
 */</doc>
<def f='master/drivers/bus/vmbus/vmbus_channel.c' l='236' ll='280' type='int rte_vmbus_chan_recv(struct vmbus_channel * chan, void * data, uint32_t * len, uint64_t * request_id)'/>
<use f='master/drivers/net/netvsc/hn_nvs.c' l='80' u='c' c='hn_nvs_execute'/>
