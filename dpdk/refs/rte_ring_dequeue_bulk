<def f='dpdk_1805/lib/librte_ring/rte_ring.h' l='604' ll='610' type='unsigned int rte_ring_dequeue_bulk(struct rte_ring * r, void ** obj_table, unsigned int n, unsigned int * available)'/>
<use f='dpdk_1805/lib/librte_ring/rte_ring.h' l='670' u='c' c='rte_ring_dequeue'/>
<doc f='dpdk_1805/lib/librte_ring/rte_ring.h' l='585'>/**
 * Dequeue several objects from a ring.
 *
 * This function calls the multi-consumers or the single-consumer
 * version, depending on the default behaviour that was specified at
 * ring creation time (see flags).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects) that will be filled.
 * @param n
 *   The number of objects to dequeue from the ring to the obj_table.
 * @param available
 *   If non-NULL, returns the number of remaining ring entries after the
 *   dequeue has finished.
 * @return
 *   The number of objects dequeued, either 0 or n
 */</doc>
<use f='dpdk_1805/drivers/mempool/bucket/rte_mempool_bucket.c' l='185' u='c' c='bucket_dequeue_orphans'/>
<use f='dpdk_1805/drivers/mempool/bucket/rte_mempool_bucket.c' l='327' u='c' c='bucket_dequeue_contig_blocks'/>
