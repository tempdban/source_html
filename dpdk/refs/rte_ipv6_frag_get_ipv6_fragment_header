<def f='master/lib/librte_ip_frag/rte_ip_frag.h' l='227' ll='235' type='struct ipv6_extension_fragment * rte_ipv6_frag_get_ipv6_fragment_header(struct ipv6_hdr * hdr)'/>
<doc f='master/lib/librte_ip_frag/rte_ip_frag.h' l='216'>/**
 * Return a pointer to the packet&apos;s fragment header, if found.
 * It only looks at the extension header that&apos;s right after the fixed IPv6
 * header, and doesn&apos;t follow the whole chain of extension headers.
 *
 * @param hdr
 *   Pointer to the IPv6 header.
 * @return
 *   Pointer to the IPv6 fragment extension header, or NULL if it&apos;s not
 *   present.
 */</doc>
<use f='master/lib/librte_port/rte_port_ras.c' l='189' u='c' c='process_ipv6'/>
