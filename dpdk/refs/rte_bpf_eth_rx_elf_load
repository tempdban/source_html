<dec f='master/lib/librte_bpf/rte_bpf_ethdev.h' l='84' type='int rte_bpf_eth_rx_elf_load(uint16_t port, uint16_t queue, const struct rte_bpf_prm * prm, const char * fname, const char * sname, uint32_t flags)'/>
<use f='master/app/test-pmd/bpf_cmd.c' l='109' u='c' c='cmd_operate_bpf_ld_parsed'/>
<doc f='master/lib/librte_bpf/rte_bpf_ethdev.h' l='63'>/**
 * Load BPF program from the ELF file and install callback to execute it
 * on given RX port/queue.
 *
 * @param port
 *   The identifier of the ethernet port
 * @param queue
 *   The identifier of the RX queue on the given port
 * @param fname
 *  Pathname for a ELF file.
 * @param sname
 *  Name of the executable section within the file to load.
 * @param prm
 *  Parameters used to create and initialise the BPF exeution context.
 * @param flags
 *  Flags that define expected expected behavior of the loaded filter
 *  (i.e. jited/non-jited version to use).
 * @return
 *   Zero on successful completion or negative error code otherwise.
 */</doc>
<def f='master/lib/librte_bpf/bpf_pkt.c' l='575' ll='589' macro='1' type='int rte_bpf_eth_rx_elf_load(uint16_t port, uint16_t queue, const struct rte_bpf_prm * prm, const char * fname, const char * sname, uint32_t flags)'/>
