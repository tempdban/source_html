<def f='dpdk_1805/lib/librte_ring/rte_ring.h' l='577' ll='583' type='unsigned int rte_ring_sc_dequeue_bulk(struct rte_ring * r, void ** obj_table, unsigned int n, unsigned int * available)'/>
<use f='dpdk_1805/lib/librte_ring/rte_ring.h' l='648' u='c' c='rte_ring_sc_dequeue'/>
<doc f='dpdk_1805/lib/librte_ring/rte_ring.h' l='561'>/**
 * Dequeue several objects from a ring (NOT multi-consumers safe).
 *
 * @param r
 *   A pointer to the ring structure.
 * @param obj_table
 *   A pointer to a table of void * pointers (objects) that will be filled.
 * @param n
 *   The number of objects to dequeue from the ring to the obj_table,
 *   must be strictly positive.
 * @param available
 *   If non-NULL, returns the number of remaining ring entries after the
 *   dequeue has finished.
 * @return
 *   The number of objects dequeued, either 0 or n
 */</doc>
<use f='dpdk_1805/drivers/crypto/scheduler/scheduler_pmd_private.h' l='108' u='c' c='scheduler_order_drain'/>
<use f='dpdk_1805/drivers/crypto/scheduler/scheduler_multicore.c' l='133' u='c' c='schedule_dequeue_ordering'/>
<use f='dpdk_1805/drivers/mempool/ring/rte_mempool_ring.c' l='38' u='c' c='common_ring_sc_dequeue'/>
