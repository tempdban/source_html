<dec f='dpdk_1805/drivers/crypto/virtio/virtqueue.h' l='29'/>
<doc f='dpdk_1805/drivers/crypto/virtio/virtqueue.h' l='22'>/*
 * Per virtio_config.h in Linux.
 *     For virtio_pci on SMP, we don&apos;t need to order with respect to MMIO
 *     accesses through relaxed memory I/O windows, so smp_mb() et al are
 *     sufficient.
 *
 */</doc>
<dec f='dpdk_1805/drivers/net/virtio/virtqueue.h' l='28'/>
<doc f='dpdk_1805/drivers/net/virtio/virtqueue.h' l='21'>/*
 * Per virtio_config.h in Linux.
 *     For virtio_pci on SMP, we don&apos;t need to order with respect to MMIO
 *     accesses through relaxed memory I/O windows, so smp_mb() et al are
 *     sufficient.
 *
 */</doc>
