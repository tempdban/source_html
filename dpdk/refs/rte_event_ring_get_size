<def f='dpdk_1805/lib/librte_eventdev/rte_event_ring.h' l='259' ll='263' type='unsigned int rte_event_ring_get_size(const struct rte_event_ring * r)'/>
<doc f='dpdk_1805/lib/librte_eventdev/rte_event_ring.h' l='249'>/**
 * Return the size of the event ring.
 *
 * @param r
 *   A pointer to the ring structure.
 * @return
 *   The size of the data store used by the ring.
 *   NOTE: this is not the same as the usable space in the ring. To query that
 *   use ``rte_ring_get_capacity()``.
 */</doc>
