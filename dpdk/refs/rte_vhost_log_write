<dec f='master/lib/librte_vhost/rte_vhost.h' l='226' type='void rte_vhost_log_write(int vid, uint64_t addr, uint64_t len)'/>
<doc f='master/lib/librte_vhost/rte_vhost.h' l='208'>/**
 * Log the memory write start with given address.
 *
 * This function only need be invoked when the live migration starts.
 * Therefore, we won&apos;t need call it at all in the most of time. For
 * making the performance impact be minimum, it&apos;s suggested to do a
 * check before calling it:
 *
 *        if (unlikely(RTE_VHOST_NEED_LOG(features)))
 *                rte_vhost_log_write(vid, addr, len);
 *
 * @param vid
 *  vhost device ID
 * @param addr
 *  the starting address for write
 * @param len
 *  the length to write
 */</doc>
<def f='master/lib/librte_vhost/vhost.c' l='704' ll='713' type='void rte_vhost_log_write(int vid, uint64_t addr, uint64_t len)'/>
